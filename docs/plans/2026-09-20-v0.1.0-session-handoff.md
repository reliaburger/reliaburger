# 0.1.0 session handoff — 20 September 2026

## Current continuation: clustered activation in progress

Current commits: `c4fd15a` activation plan; `98b8b42` exact release-permission
replay (3 tests, 0.258s); `8c7f7c6` council VIP registration and guarded local
reservation retirement (10 tests, 1.042s); `fb2807e` rootless execution witnesses
(36 tests, 1.233s). Current formats are protocol 20/state 39/discovery schema 4.
These supersede the earlier consumer checkpoint's state 38/schema 3.

Startup cleanup now defers stopped cluster executions until the API/receipt loop
can run. It retains original execution and host-port reservations and fences
readiness/deployment until cleanup and empty-service retirement succeed. Rootless
adoption accepts a positively confirmed owned host forward without a container IP.
Both meaningful regressions failed first; all 18 selected native startup, consumer,
producer and adoption contracts pass (1.406s), `/tmp/rb-cluster-startup-green.log`.
The pipeline has finished. Linux and final strict checks remain pending.

Hosted CI at previous remote head `146dee9` exposed two separate gaps: the authz
route matrix omits discovery retire/withdrawn endpoints; the concurrent node-fault
placement fixture assumes a leader is mapped in every peer's live view immediately.
Repair and qualify these without weakening production fault admission.

Normal startup selection is now implemented: rootful Runc/eBPF clusters recover
under enrolled node ID + SHA-256(root CA), requiring the cluster service authority;
standalone rootless Runc recovers owned forwarding without claiming kernel hooks.
Both physical startup regressions fail first; rootless Bun crash/adoption/cleanup
passes (12.94s), enrolled cluster startup/restart passes (3.53s). Logs:
`/tmp/rb-startup-green-linux.log`, `/tmp/rb-cluster-startup-green.log`.
Discovery polling during pending deployments is committed (see current git log); 37 orchestration + three actual TLS contracts pass (10.177s).

Actual standalone owned upgrade + explicit rollback now pass: rootful 15.32s,
rootless (including crash/forward recovery) 25.09s. Logs:
`/tmp/rb-owned-upgrade-linux.log`, `/tmp/rb-activation-rootful.log`.
The complete single-node enrolled cluster lifecycle now passes: council publication,
Bun death, adoption with unchanged PID/port, desired-app removal and eventual
service/address/receipt retirement. This first exposed non-idempotent backend
removal on retry. Fixed while retaining consumer invalidation and remote proof.
Evidence: `/tmp/rb-cluster-lifecycle-green.log` (preceding failure in
`/tmp/rb-cluster-lifecycle.log`). All pipelines have finished.

Failed-candidate OCI revert now passes on rootless (32.06s) and rootful (23.83s),
including successful upgrade and explicit rollback first. Cluster lifecycle repeats
successfully (18.17s). Logs: `/tmp/rb-owned-revert-linux.log`,
`/tmp/rb-owned-revert-rootful.log`. No active pipelines.
The disposable VM is nearly full from accumulated Rust test binaries (35GB deps);
reclaim only rebuildable package artifacts before compiling further.

Pending: multi-node clustered rolling qualification,
final affected platform/strict checks and the known placement fixture CI race. Rootless
cluster scope remains an unanswered async question: standalone rootless with
rootful clusters is recommended for 0.1.0. No answer has arrived; rootless cluster
production selection remains unchanged/unqualified. All test pipelines are reaped.
See `2026-09-22-cluster-activation.md`. Do not mark C34 complete yet.



## Earlier checkpoint: durable consumer loop implemented

Commits: `77e135f` (phase/receipt journal), `82066b7` (agent recovery/withdrawal),
`acc9b60` (authenticated retry), `d9db398` (real kernel/TLS qualification),
`ef19fb8` (ready receipt fallback regression). All implementation commits are pushed
to PR167. The final documentation commit follows this checkpoint.

Completed: journal original catalogue/merged services/ingress before publication;
withdraw the complete previous view; wait for positive HTTP/WebSocket guard release;
compact only after durable withdrawal; preserve generation fences and exact receipt
instructions; recover under the original enrolled identity with empty userspace
views; retry through the current authenticated leader; forget only after exact 204
and explicit journal confirmation. Failed publication still returns ready receipts.
Cluster clients disable redirects. Updates can briefly interrupt routing by design.
Protocol 20/state 38, discovery schema 3. Other ownership schemas are unchanged.

Final-source validation: 406 affected library tests on each platform
(native 18.962s, Linux 34.060s); three real API TLS tests per platform
(native 4.155s, Linux 4.550s); two focused physical kernel tests (0.23s).
All 80 physical kernel tests pass (182.57s). Strict all-target/all-feature Clippy
and formatting pass on both platforms; the Linux build without default features
also passes. All native/Linux compiler and test pipelines have finished.
Logs: `/tmp/rb-consumer-final-native.log`, `/tmp/rb-consumer-tls.log`,
`/tmp/rb-consumer-kernel.log`, `/tmp/rb-consumer-final-linux.log`.
The transport tests first failed twice (`/tmp/rb-consumer-transport-red.log`).
A test build initially omitted the feature exposing MockGrill; corrected by gating
that integration on `ebpf` and running the feature on both platforms.

Next work is **clustered production activation**, not more consumer machinery:

1. In `src/bin/bun.rs`, wire normal clustered owned Runc/kernel startup to
   `recover_consumer_ownership` using enrolled node ID and the stable root trust
   fingerprint. Recover before proxy/DNS/adoption/readiness. Preserve ownership
   mode-change and lost-authority refusals. Current normal durable selection is
   standalone rootful Runc with eBPF only.
2. Complete clustered service VIP retirement and original local runtime correlation;
   do not simply remove the cluster refusal in `retire_discovery_service`.
   Empty cluster allocations currently remain reserved. Prove committed consumer
   withdrawal before physical release, including interrupted release/recovery.
3. Qualify rootless integration, actual upgrade/rollback and the complete supported
   clustered lifecycle. Then V01–V04: live three-node matrix, sustained recovery,
   exact signed candidate and repeated cold laptop installs.

Use `docs/plans/2026-09-22-v0.1.0-remaining-work.md` for estimates (3–6 engineering
days plus waits/soak). Do not merge, tag, release or declare 0.1.0 ready yet.
At `ef19fb8`, hosted CI is running. The final documentation push requires a fresh
head/check inspection; do not describe pending or absent checks as green.
Only the main checkout exists. No worktrees or private-key changes this session.
Always use `CARGO_INCREMENTAL=0`; never edit shared Rust sources while native or
Linux compilation/test pipelines run. Reap the pipeline first.

**Not ready to release.** The remaining correctness work is C34, followed by
acceptance gates V01–V04. The installer, website, signing workflow and many
correctness fixes exist, but their existence is not final release qualification.

Continue on `codex/codebase-completion-fixes`, draft
[PR #167](https://github.com/reliaburger/reliaburger/pull/167). PRs #165 and #166
were merged. Keep one commit per fix or feature. Do not start another branch,
merge, tag or publish as part of resuming this checkpoint.

**Active checkout:** `/Users/miko/github/reliaburger`, on
`codex/codebase-completion-fixes`. At the user's request, release work is back in
the original checkout and the temporary `.worktrees/release-0.1.0` worktree has
been removed. The merged keynote changes are included (`5b68c95`), followed by
the tested consumer-registration change (`72100b2`). Its original duplicate
commit `6ce187f` remains on the keynote branch; no work was lost or reset.

All local test pipelines finished before moving. Resume ordinary native builds
here with `CARGO_INCREMENTAL=0`; Linux uses this same source path and
`CARGO_TARGET_DIR=/home/miko.guest/reliaburger-release-target`. All secondary worktrees have now been removed. The unrelated July worktree had
13 branch commits and unfinished embedded-test-workload changes. They are safely
preserved locally as `codex/archive-medium-value-worktree-2026-09-22` (`c961c2f`),
a stash-shaped commit: its first parent retains the original branch history and
its third parent retains the untracked sources/binaries. The original
`codex/medium-value-hardening-2026-07-18` branch also remains. Only ignored compiler
output was discarded. These old changes have not been integrated into 0.1.0;
the user was asked whether to do so and has not requested integration. Do not apply this archive over the release
checkout without reviewing the old changes against current implementations.

The continuations below supersede the earlier `51b2ee8` checkpoint. Hosted
checks always need inspection for the current commit.

## Earlier checkpoint: consumer agent recovery and withdrawal

The new `src/bun/agent/consumer.rs` owns merged publication, full-view withdrawal,
request-guard drainage, generation-preserving compaction and ready-receipt retention.
`recover_consumer_ownership` verifies enrolled node/fingerprint before reconciling
local runtime and combined local/remote kernel evidence. Normal local lifecycle
updates invalidate the consumer view; the consumer command exclusively publishes
it. Local endpoints require committed allocations and matching runtime generations.
`SyncClusterConsumer` returns publication confirmation and ready receipts;
`ConfirmConsumerReceipt` forgets only a ready acknowledged generation. Already-ready
receipts can progress even when a new publication is refused (e.g. capacity).

Tests: 33 native consumer/storage/publication cases pass (0.845s), final two
HTTP/WebSocket drainage/recovery cases pass (0.397s). Logs:
`/tmp/rb-consumer-agent-commands.log`, `/tmp/rb-consumer-agent-final.log`.
All compilers finished. Next implement authenticated transport and placement
reconciler retry, then physical kernel and broader platform qualification. Cluster
production activation is still separate; these APIs are not yet selected by normal
clustered Bun startup. State 38/discovery 3, protocol 20.

## Earlier checkpoint: consumer phase and receipt journal

Protocol 20/state 38, discovery schema 3. Consumer snapshots now include ingress;
Publishing/Active/Withdrawing/Withdrawn phases gate compaction, preserving the
latest generation fence. Exact withdrawal instructions carry Pending/Ready receipt
state. Pending receipts cannot be forgotten or sent; promotion requires a prior
Withdrawn checkpoint. Two regressions fail first, then 30 native consumer,
discovery-storage and compatibility tests pass (0.572s). Logs:
`/tmp/rb-consumer-storage-{red,green}.log`. Existing fixture snapshots were updated
to include the new fields after their expected-JSON comparisons failed.

Continue with `docs/plans/2026-09-22-consumer-recovery.md`: wire attempted publication,
request drainage, startup recovery and authenticated receipt retry. The live loop
is not yet implemented at this checkpoint. All compilers have finished. Do not
activate clustered production by bypassing remaining consumer/runtime gates.

## Earlier checkpoint: kernel/discovery session complete

Implementation commits: `14ca2dc` (boot-bound kernel recovery), `e72e440` (normal
standalone activation), `edb92bb` (whole Bun/kernel/discovery reboot driver), and
`639c1b2` (missing authority refusal). Protocol 20/state 37, kernel manifest 3;
other schemas unchanged. Only the main checkout is used.

Final-source qualification after the last fix:

- Real VM power-cycle passes with identical Bun/test checksums; verification
  3.97s. Host log `/tmp/rb-discovery-reboot-authority.log`, guest evidence
  `/var/tmp/reliaburger-discovery-reboot.qVUedS`, boot
  `6efd6d05-9c92-401c-ad90-b9a04ee97df0`.
- All **78 physical kernel tests pass**, 182.25s. Strict all-target/all-feature
  Linux Clippy passes (65s), Bun without default features builds (45.67s), and
  formatting passes. `/tmp/rb-kernel-authority-final-resume.log`.
- Before the final Linux-only refusal fix, the complete OCI interruption driver
  passes (seven actual Bun boundaries plus normal standalone recovery), all 76
  then-existing kernel tests pass, and 28 discovery/compatibility plus seven
  real startup/API tests pass on each platform. Native strict Clippy/formatting
  pass; see `/tmp/rb-discovery-final-{native,linux}.log`.
- The final post-reboot shell initially referenced an artifact list under guest
  `/tmp`; reboot had correctly cleared it. Regenerated the list under `/var/tmp`
  and completed all checks. No source change was needed for this test-driver
  issue. Every build/test pipeline has finished; shared Rust sources can be edited.

Normal **standalone rootful Runc with eBPF configured** now uses durable runtime,
kernel and discovery ownership. This is not whole-cluster activation. Next finish
consumer publication/recovery, confirmed withdrawal, receipt retry and compaction;
then enable clustered/rootless production recovery and qualify actual
upgrade/rollback. V01–V04 remain. See the updated remaining-work table (roughly
4–9 engineering days plus waits/soak). No release, tag or candidate was published.
Final-head hosted CI must be inspected separately; it is not included in local
qualification claims. The last inspected pushed implementation head had no
reported check entries, so it must not be called green.

## Earlier checkpoint: lost kernel authority refuses recovery

Both missing-authority regressions failed first: a missing lock admitted another
live owner; a missing retired manifest permitted reactivation. Only a newly
created private state directory can now initialise a lock and manifest. Existing
journals require original files. Fresh files, directory and parent are synced;
volatile prerequisites are checked before creating ownership. Eight physical
persistent-policy cases pass (`/tmp/rb-kernel-claim-green.log`); the two expected
failures are retained in `/tmp/rb-kernel-claim-red.log`.

Preceding broad qualification passes 76 physical kernel tests (184.23s), the full
OCI driver (including seven interruption boundaries and normal durable startup),
28 discovery/compatibility plus seven actual startup/API tests on each platform,
and strict all-target/all-feature Clippy/formatting on both. Logs:
`/tmp/rb-discovery-final-native.log`, `/tmp/rb-discovery-final-linux.log`.
Next: final-source reboot, physical kernel and Linux lint confirmation after this
Linux-only refusal fix, then push/update PR and final handoff. Formats remain
protocol 20/state 37. C34 clustered/rootless activation and V01–V04 remain open.

## Earlier checkpoint: complete standalone kernel/discovery reboot qualification

`scripts/release/qualify-discovery-reboot.sh --vm reliaburger-test` now passes end
to end with identical Bun/test executable checksums. Normal standalone rootful
Runc/eBPF activation preserves discovery obligations over a power-cut, recreates
vanished pins only under a new boot, retires the interrupted workload without
republishing it, clears the original service/reference, permits explicit
redeployment, refuses stale release and adopts the successor on another Bun
restart. Verification passes in 3.94s. Final guest evidence:
`/var/tmp/reliaburger-discovery-reboot.yJcTZE`; host log:
`/tmp/rb-discovery-reboot-final.log`; new boot:
`8cf5ed9e-09ce-45e6-b05e-ff4fef658a53`.

Earlier attempts remain documented: the fixture raced runtime polling, then the
post-boot test mount lacked `/run/netns`. It now reads the committed discovery
checkpoint and creates that volatile mount point. The first actual reboot passed
on resumed verification (4.06s, checksums unchanged); final run verifies the
whole corrected driver. The failed live fixture was recovered and retired via
Bun after reboot (`/tmp/rb-discovery-fixture-cleanup.log`, 0.38s), never by saved
PID. Rootless AppArmor allowance resets on reboot; its host default remains 1.

Next: broader kernel/OCI/startup tests and strict checks. Cluster production
activation depends on durable consumer integration; rootless and actual
upgrade/rollback qualification remain open. C34 and V01–V04 are not complete.

## Earlier checkpoint: standalone production startup

Normal standalone rootful Runc with eBPF enabled now selects owned execution,
persistent kernel maps/links and durable discovery recovery before adoption.
Mounted bpffs is required. Existing ownership prevents bypass through runtime,
cluster or enforcement mode changes; recovery errors prevent readiness.

The actual Bun test fails first (`/tmp/rb-durable-bun-red.log`), then passes
crash/adoption/retirement plus disabled-enforcement and missing-discovery refusal
(`/tmp/rb-durable-bun-refusal.log`, 3.88s). Next qualify whole-VM reboot through
this complete startup path and run broader checks. Cluster consumer integration,
rootless production activation and upgrade/rollback remain unfinished.

## Earlier checkpoint: kernel boot identity

Persistent kernel manifests now require boot UUID and schema 3; protocol 20/state
37. Prior-boot recreation requires unchanged configured paths and an empty pin
inventory. Same-boot missing pins and prior-boot live conflicts refuse recovery.
Retirement cannot turn back into activation. The physical regression fails first
(`/tmp/rb-kernel-boot-red.log`, 5 pass/1 fail); six physical persistent-policy cases
pass in 1.34s (`/tmp/rb-kernel-boot-green.log`).

Next: wire and qualify complete standalone Bun/kernel/discovery startup, including
actual VM reboot. Cluster production activation still depends on the unfinished
consumer publication/recovery/withdrawal loop. Do not bypass that dependency.

## Earlier checkpoint: actual OCI interruption and reboot qualification

Implementation commits are saved for PR #167:
`dcbdd2a` (boot-bound command owners), `c0c5c76` (boot-bound OCI recovery/driver),
`9ec73ef` (actual reboot evidence), `f151e94` (actual Bun crash cases),
`0d9b75d` (rolling init fix), `cfec14b` (pre-intent/retry qualification), and
`577b899` (privileged CI gate). Compatibility is **protocol 20/state 36**, process
owner schema 3, OCI intent 5, discovery 2, lease 5 and job checkpoint 2.

Seven actual Bun SIGKILL/cancelled-caller cases pass: pre-intent admission,
preparation, launch, running init, retirement, adoption and rolling retry.
The pre-intent case intercepts the actual lock-file open with a test-only compiled
shim and proves no initialiser intent exists before Bun dies. Recovery prevents
unadopted chains from advancing and preserves adopted execution. Each case then
runs an explicit successful retry. The suite exposed rolling replacements
skipping initialisers; fresh and rolling workers now share the init sequence.

The actual abrupt VM power-cycle also passes: changed boot UUID, absent original
kernel resources, retained stale OCI state, unknown interrupted outcome, cancelled
prepared launch, retained address ownership, explicit release, fresh execution and
stale-release refusal. Before/after test binary checksums match. This proves the
rootful runtime boundary; it does **not** close full pinned-kernel/consumer recovery
or production activation. The hidden standalone-only `--experimental-owned-runc`
option exists for qualification and refuses cluster mode.

Final local results:

| Check | Result |
| --- | --- |
| Affected library tests | 476 native (18.328s), 521 Linux (29.230s) passed |
| Runtime/owner/recovery integrations | 91 native (27.003s), 94 Linux (23.742s) passed |
| Privileged network and owned Runc suites | 4 / 14 harness cases passed (1.43s / 12.93s), including gated fixture helpers |
| Actual Bun interruption matrix | All seven boundaries passed (37.93s in the driver; earlier focused run 34.41s) |
| Actual VM reboot | Prepare 1.06s; verify after reboot 1.30s; different kernel UUID |
| Physical eBPF suite | 74 passed (184.80s) |
| Rootless owned runtime suite | 6 harness cases passed (3.33s), including the subprocess fixture |
| Strict all-target/all-feature Clippy and formatting | Passed on both platforms |

Evidence is in `/tmp/rb-oci-final-{native,linux}.log`,
`/tmp/rb-oci-native-refusal-rerun.log`, `/tmp/rb-oci-final-linux-resume.log`,
`/tmp/rb-oci-portable-linux.log`, `/tmp/rb-oci-crash-complete-linux.log` and
`/tmp/rb-actual-oci-reboot.log`. The guest interruption driver retains artifacts
under `/var/tmp/reliaburger-oci-interruptions.UjfHYP`; the reboot fixture is
`/var/tmp/reliaburger-reboot.y1jbtF` and its host evidence directory is recorded
below. Fixtures deliberately retain diagnostic files. The live owner left by the
failed rolling-init fixture was retired through its authenticated control socket;
its terminal record was confirmed, without signalling a recovered PID.

Initial qualification failures are preserved, not counted as passes. One native
role-validation case hit a transient broken control socket; its focused rerun and
complete 91-case rerun pass on unchanged code. The VM reboot restored AppArmor's
unprivileged-userns restriction; rootless cases initially failed at namespace
creation, then all passed with the CI prerequisite enabled temporarily. The
original setting (`1`) was restored. Reboot also removed `/tmp`'s nextest runner;
official 0.9.145 was downloaded, SHA-256 verified and installed under
`/var/tmp/rb-nextest-0.9.145.*/cargo-nextest`. Use that path in subsequent sessions.

Next: complete the durable consumer publication/recovery/withdrawal/receipt loop
and service VIP retirement, then reconcile pinned kernel/discovery ownership
across reboot and select the complete production path. Remaining runtime variants,
upgrade/rollback, V01–V04 and final-head hosted CI are still acceptance gates.
The updated [remaining-work table](2026-09-22-v0.1.0-remaining-work.md) estimates
5–10 engineering days plus external waits and the 24–48 hour soak. All local
pipelines have finished and been reaped. The main checkout is the only worktree. No merge,
candidate, tag or release has been performed.

## Earlier checkpoint: rolling initialisers

The seventh actual Bun boundary found that rolling replacements skipped init
containers entirely. The fix shares `drive_initialisers` between fresh and rolling
workers, including admission, confirmed retirement and policy refresh. Thirteen
native rolling/init contracts pass (1.093s), including failed-init containment.
The seven-case real OCI rerun passes (34.41s), including the retry that exposed the bug; the fixture uses a test-only C
LD_PRELOAD gate before initialiser intent admission, rather than a production
fault hook. That pipeline has finished. Broad native/Linux and physical runtime/kernel qualification is next.

## Earlier checkpoint: reboot recovery foundation

`dcbdd2a` binds command owners to Linux boot UUIDs. Final command tests pass:
14 macOS cases (6.884s), 16 Linux cases (0.595s). OCI intent now also records the
original boot (schema 5, protocol 20/state 36). A prior-boot launch cannot start;
cleanup first refuses conflicting current kernel cgroups/namespaces/veths, drains
owned commands, and removes stale private OCI metadata without signalling saved
PIDs. Discovery address holds remain independent.

The real-Runc conflict regression fails first for missing intent boot evidence and
passes after implementation. A failed first test leaked its own namespace/veth;
those exact objects were identified and removed, and the fixture now cleans up
before asserting. All 89 affected native contracts pass (27.497s), with strict
Clippy and formatting. The two-phase actual-reboot fixture/driver is implemented;
actual VM power-cycle passed on 22 September: prepare 1.06s, post-reboot verify
1.30s. Boot changed from `d32c179b-5f97-410f-8af3-c9701c6ffff8` to
`d8636341-381d-4d15-9ac4-eb0e70f44efa`. Evidence:
`/tmp/rb-actual-oci-reboot.log`, host temporary evidence directory
`/var/folders/2_/8114g2p575n5_8_3x1p7484c0000gn/T/reliaburger-oci-reboot.m66HyF`,
and guest `/var/tmp/reliaburger-reboot.y1jbtF`. Exact test binary checksum matches
before and after reboot. Broad Linux qualification and the actual Bun matrix
follow. The first five actual Bun interruption cases pass (78.97s): preparation,
start admission, running init, namespace retirement and main adoption, each with
explicit retry. The hidden `--experimental-owned-runc` switch refuses cluster and
non-Runc use. Logs: `/tmp/rb-oci-crash-green-linux.log`; individual guest fixture
directories are printed there and retained. Earlier admission and death during a rolling
retry now pass in the seven-case matrix (34.41s); logs are in
`/tmp/rb-oci-crash-complete-linux.log`. Do not mark the
whole OCI interruption, kernel/discovery reboot or production-selection gates done.

## Earlier checkpoint: producer release (22 September)

**Producer address release and stale-report protection are implemented in the
opt-in durable discovery path.** Early commits: `887c77a` (permanent Raft execution
fences and atomic withdrawal) and `ac76b5c` (authenticated API and agent release
gate). Follow-ups: `c609b4f` (test-helper ordering) and `968bd3e` (typed refusals).
Protocol 20/state 34 replace 19/33; discovery schema 2 and lease schema 5 remain.

Both scheduler and Raft reject retired execution reports, including missing
generation evidence from fenced producers. Release checks every historical
consumer obligation; permanently fenced decommission discharges only that node.
The API derives the producer from current TLS, requires system authority and a
quorum-backed security read, returns 202 while pending, and names the exact
producer/execution on confirmation. The bounded HTTPS client verifies that result.
Bun correlates original runtime inventory before deleting records, returning
process host ports or releasing Runc references. Runc's local durable permission
still precedes runtime release. Cancelled/failed requests retain original ownership;
automatic restart uses the same gate. The binary supplies enrolled transport.

Failing-first evidence: three Raft contracts (0.084s), three HTTP contracts
(0.058s), two agent/client contracts (0.103s). Final focused tests pass nine cases
on native/Linux (0.384s/0.643s); all six receipt/producer API cases pass (0.057s).
Broader native/Linux qualification passes 770/782 library cases (18.584s/32.414s),
sixteen integrations each (5.941s/5.018s), 45 recovery/runtime cases each
(26.071s/23.710s; one explicit native skip), and three real failover/decommission
cases each (71.241s/71.081s; larger ignored failover case unselected). Strict
all-target/all-feature Clippy and formatting pass after the test-helper ordering
fix. Both original and final pipelines are reaped; no Rust builds remain active.
Logs/scripts: `/tmp/rb-producer-release-{native,linux}.{log,sh}` and
`/tmp/rb-producer-release-final-{native,linux}.{log,sh}`. Earlier focused logs are
`/tmp/rb-producer-{retirement,api,agent}-{red,focused}.log`. Local qualification is
not a claim of completed hosted CI; inspect the current head independently.

**Next:** consumer publication/cluster recovery must persist original service and
ingress exposures, restore generation fences, confirm request drainage and submit
replayable withdrawal receipts. Service VIP retirement and safe consumer-history
compaction remain open. Then qualify real OCI crash/cancellation/reboot boundaries,
select the durable runtime/kernel/discovery profile in production, and finish
V01–V04. The new producer gate is conditional on the opt-in durable discovery
profile; existing real-cluster tests do not establish the unfinished full consumer
cleanup loop. Do not enable production selection or mark C34 complete yet.

The [remaining-work table](2026-09-22-v0.1.0-remaining-work.md) records difficulty,
completion evidence and an updated 5–12 engineering-day working estimate plus
external waits/soak time. No merge, candidate, tag or release was created.

### Initial Raft checkpoint

Producer release work is in progress. The first checkpoint implements permanent
producer execution fences in Raft, atomic removal/withdrawal recording, and a
release result that checks every historical consumer obligation. Both scheduler
and Raft reject retired executions and missing-generation reports from fenced
producers. A new execution may reuse the original instance/address after release.
Three contracts fail first (0.084s), then pass; snapshot replay, never-published
executions, offline consumers, decommission and full-capacity refusal are covered.
Protocol 20/state 34 replace 19/33. HTTP authentication and agent release integration
are next in this session; do not mark the producer work package complete yet.
Logs: `/tmp/rb-producer-retirement-{red,focused}.log`. No Rust pipeline is active.

### Previous consumer generation checkpoint

Latest feature: **Consumer catalogue generation fence and merged-view validation**
(`edf551a`, committed and pushed early).
The HTTP reconciler preserves the committed generation in `SyncClusterCatalog`.
Bun refuses stale or same-generation rewritten catalogues, advances on identical
newer contents, and leaves the fence unchanged on refusal. Generation zero requires
an empty catalogue. The effective merged local/remote map is validated before any
publication. This is an in-memory fence; durable recovery still needs to restore it.
Unchanged catalogue generations can carry independent ingress configuration changes;
the catalogue fence does not order those changes.

Four contracts fail first (0.095s). All eight focused cases pass (4.057s), including
real HTTP propagation. The first focused run passed seven; the HTTP fixture was
blocked by the sandbox's socket restriction, then passed with local socket access.
Native/Linux qualification passes 490/502 affected library cases
(18.398s/32.257s), thirteen integration cases each (5.121s/5.053s), three real
failover/decommission cases each (71.409s/71.044s), strict all-target/all-feature
Clippy and formatting. Both pipelines are reaped; no Rust builds remain active.
The larger ignored failover case was not selected. Logs:
`/tmp/rb-consumer-generation-{red,focused,native,linux}.log`; scripts:
`/tmp/rb-consumer-generation-{native,linux}.sh`. Hosted CI at `edf551a` had three
successes, sixteen running and one skipped at inspection; inspect current-head
checks separately.
Protocol 19/state 33 and discovery schema 2 are unchanged.

Remaining: durable publication/cluster recovery, original ingress exposure capture,
confirmed drainage, receipt retry/compaction, producer release and stale-report
fencing, then actual crash/reboot and production-path qualification. C34 and
V01–V04 remain open. The [remaining-work table](2026-09-22-v0.1.0-remaining-work.md)
records difficulty, completion evidence and estimated effort.

### Previous consumer journal checkpoint

Latest feature: **Durable consumer ownership storage** (`6b105b0`, committed and pushed early). The exclusive discovery
journal retains an enrolment-bound identity, original committed catalogues and
exact proposed merged service views. Immutable history and bounded capacity
prevent uncertain ownership from disappearing. Standalone recovery and fresh
enablement refuse consumer evidence. This store is not yet called by cluster
publication; no new receipt or producer-release authority is enabled.

Five initial contracts fail first (0.157s). The first implementation run passes
20/21: a test incorrectly treated a printable node ID as a filesystem path; node
IDs follow the existing printable-name contract, so the invalid fixture now uses
an empty identity. All 24 journal/fresh-enable cases pass (0.711s), including total
exposure capacity and local-view updates within one generation. State advances to
33, discovery schema to 2; protocol remains 19 and lease schema 5.

Native/Linux qualification passes 349/361 affected library tests
(18.497s/23.954s), thirteen integration tests each (5.487s/5.025s), strict
all-target/all-feature Clippy and formatting. Both pipelines are reaped; no Rust
builds remain active. No additional physical-kernel or failover rerun was needed
for this storage-only change. Scripts and logs:
`/tmp/rb-consumer-ownership-{native,linux}.sh` and matching `.log` files;
red/focused evidence: `/tmp/rb-consumer-ownership-{red,focused}.log`.
Hosted CI at `6b105b0` had two successes, seventeen running and one skipped
at inspection. Local qualification is not a claim of completed hosted CI.

Next: connect generation/instructions and stable enrolled identity to attempted
publication; persist original service/ingress exposures before publication and
recover them before new publication. Add confirmed-withdrawal/receipt phases and
safe compaction (the present store refuses at capacity). Then integrate receipt
retries and producer release with stale-report fencing. C34 and V01–V04 stay open.
See the latest storage checkpoint in the remote-withdrawal plan.

### Previous checked publication checkpoint

Latest completed feature: **Checked atomic cluster publication** (`0e80491`,
committed early; fake-agent follow-up `866a52f`). Bun validates catalogue
allocations and builds a candidate routing table before replacing catalogue,
DNS and ingress views. Refusal preserves the previous publication. The placement
loop requires a checked oneshot result, with its deadline covering both queueing
and confirmation; refused or lost replies retry before placement work.

Three contracts fail first (0.115s); six focused cases pass (6.225s). The first
broader native run passed 456/457 cases: an older stalled-retirement fake agent
omitted the newly required reply. The corrected fixture passes. Final native/Linux
qualification passes 457/469 affected library cases (18.436s/31.797s), thirteen
integration cases each (5.699s/5.022s), three real failover/decommission cases each
(72.430s/71.604s), strict all-target/all-feature Clippy and formatting. Both
pipelines are reaped; no Rust builds remain active. The larger ignored failover
case was not selected. Current formats remain protocol 19/state 32, lease schema 5.

Logs: `/tmp/rb-checked-cluster-publication-{red,focused,native-first,native,linux}.log`;
Linux script: `/tmp/rb-checked-cluster-publication-linux.sh`. Hosted CI on `866a52f`
had two successes, seventeen running and one skipped at inspection. Inspect the
latest commit independently; local qualification is not a claim of completed CI.

**Next implementation:** durable consumer ownership before attempted publication.
Carry the complete generation/instructions into the agent, record exact effective
local-plus-remote exposures, and correlate them on recovery. Then confirm original
withdrawal and actual request-guard release before submitting receipts. Producer
address/host-port release still requires committed proof and stale-report fencing.
See the [consumer integration handoff](2026-09-22-remote-withdrawal-ledger.md#consumer-integration-handoff).
C34 and V01–V04 remain open. No merge, candidate, tag or release was created.

Earlier completed feature: **Authenticated generation receipts** (`000db00`,
committed and pushed early). Three HTTP/Raft contracts fail first (0.066s),
followed by two Raft snapshot/refusal contracts (0.092s). All five focused cases
pass (0.055s). Native/Linux qualification passes 404/416 affected library cases
(10.182s/10.889s), thirteen integration cases each (5.356s/5.051s), three real
failover/decommission cases each (71.987s/71.572s), strict all-target/all-feature
Clippy and formatting. Both pipelines are reaped; no Rust builds remain active.
The larger ignored failover case was not selected.

The receipt derives its consumer from current TLS identity, requires system
authority and a quorum-backed security read, and acknowledges only an Applied
write. Historical retries preserve other generations/consumers; registration
remains. Current formats: protocol 19/state 32, lease schema 5. Logs:
`/tmp/rb-withdrawal-receipts-{red,raft-red,focused,native,linux}.log`;
Linux script: `/tmp/rb-withdrawal-receipts-linux.sh`.

**Next implementation:** durable consumer ownership before publication, checked
routing withdrawal and actual captured-request guard release before receipt
submission, then producer release with stale-report fencing. The
[consumer integration handoff](2026-09-22-remote-withdrawal-ledger.md#consumer-integration-handoff)
identifies the concrete code paths and regression cases. Receipt acceptance alone
does not prove local withdrawal or enable physical address/host-port reuse.
C34 and V01–V04 remain open. No merge, candidate, tag or release was created.
Hosted CI on `000db00` had two successes, seventeen running and one skipped at
inspection; inspect the latest commit independently.

Earlier completed feature: **Consumer-specific withdrawal instructions** (`8aa0163`,
committed and pushed early after both focused tests passed in 0.057s). Placement
responses include the committed publication generation and only the requesting
consumer's original withdrawal generations, services and backend identities, all
from the same committed snapshot as the active catalogue. Missing discovery
fields now refuse decoding. Registration and authentication checks still apply;
reading instructions does not acknowledge them. Two contracts fail first (0.115s).
Native/Linux qualification passes 402/414 affected library tests (10.222s/10.798s),
ten agent/compatibility cases each (5.390s/5.014s), cross-node discovery through
leader replacement (23.341s/23.450s), strict all-target/all-feature Clippy and
formatting. Both earlier pipelines were reaped. Formats at that checkpoint:
protocol 18/state 31, lease schema 5.

Logs: `/tmp/rb-withdrawal-instructions-{red,focused,native,linux}.log`;
Linux script: `/tmp/rb-withdrawal-instructions-linux.sh`. Hosted CI on `8aa0163`
had two successes, seventeen running and one skipped at inspection; inspect the
latest commit independently. No merge, candidate, tag or release was created.

The authenticated receipt follow-up is now complete above. Next integrate
durable consumer ownership/withdrawal and producer release with stale-report
fencing. No receipt or physical release integration is implemented by this response
change. C34 and V01–V04 remain open.

Earlier completed feature: **Generation-checked catalogue publications** (`6643aed`).
PublishEndpoints requires the committed generation used to prepare its candidate;
Council refuses stale/future generations before any mutation, even for no-ops.
The scheduler supplies its observed generation. Four regressions fail first
(0.101s); all five focused tests pass (0.049s). Native/Linux qualification passes
398/410 affected library cases (10.138s/10.451s), ten agent/compatibility cases each
(5.344s/5.025s), three real failover/decommission cases each (70.827s/71.270s),
strict all-target/all-feature Clippy and formatting. Both pipelines are reaped.

Compatibility at that checkpoint: protocol 17/state 31. Logs:
`/tmp/rb-publication-generation-{red,focused,native,linux}.log`.
The consumer instructions follow-up is now complete above. Authenticated receipts
and durable consumer/producer integration remain. Physical
release and stale report republication after producer retirement remain open.
Hosted checks on `6643aed`: three passed, sixteen running, one skipped at inspection.

Earlier completed feature: **Reserve withdrawn VIPs through allocation and Raft
publication** (`2481943`). The allocator reserves current/departing allocations
and older ledger reservations; publication validates allocations and refuses
reuse against prior and newly created withdrawals atomically. Normal backend
replacement retains its active service VIP. Formats remain protocol 16/state 30.

Six contracts fail first (0.087s); fourteen focused cases pass (0.121s). Final
native/Linux qualification passes 394/406 affected library cases
(10.176s/10.513s), ten agent/compatibility cases each (5.426s/5.022s), three real
failover/decommission cases each (71.297s/70.040s), strict all-target/all-feature
Clippy and formatting. One larger ignored failover case was deliberately not
selected. Both pipelines are reaped; no Rust builds are active.
Logs: `/tmp/rb-withdrawn-vip-{red,focused,native,linux}.log`.

Next: expose generation-bound withdrawal instructions and accept authenticated
consumer receipts. Integrate durable consumer ownership, kernel/routing withdrawal
and positive request-guard release before acknowledgements. Producer release
must require the committed result and prevent stale reports from republishing a
retired execution after its address has been reused. These are still open; VIP
reservation alone does not authorise physical address/host-port reuse. See the
[remote withdrawal plan](2026-09-22-remote-withdrawal-ledger.md).

C34 and V01–V04 remain open. Hosted checks for the newest commit need inspection;
local qualification is not a claim that CI has finished. No merge, candidate,
tag or release has been published. All completed code is committed.

Earlier completed feature: **Replicated remote withdrawal obligations** (`0ea5702`).
Council records monotonically increasing publication generations and the removed
original service/backend exposures with their durable consumer census. Later
publications and snapshots retain older obligations. Unchanged backends and
health changes don't acquire spurious drains. Sequence/capacity refusal leaves
state intact. Permanent decommission discharges only the fenced consumer and
journals its produced endpoints for everyone else before changing other state.

Five pure contracts fail first (0.089s), then three Raft/decommission contracts
(0.092s). The first nine focused cases pass (0.120s); final qualification includes
two more edge cases for partial removal and changed routes. Native/Linux pass
388/400 affected library cases (10.163s/10.747s), ten agent/compatibility cases
each (5.423s/5.028s), three real failover/decommission cases each (70.843s/72.035s),
strict all-target/all-feature Clippy and formatting. Both pipelines are reaped.
Logs: `/tmp/rb-withdrawal-ledger-{native,linux}.log`,
`/tmp/rb-withdrawal-{ledger,raft}-red.log`, `/tmp/rb-withdrawal-focused.log`.

Formats at that checkpoint: protocol 16/state 30. This checkpoint does not authorise physical
address reuse or add a consumer receipt endpoint. The VIP reservation follow-up above is now complete. Stale-writer checks,
authenticated generation receipts and durable consumer/producer integration
remain. C34 and V01–V04 remain open. Current hosted checks need
inspection; local qualification is not a claim that CI has finished.

Implementation sequence: [remote withdrawal plan](2026-09-22-remote-withdrawal-ledger.md).
Checkpoint 1, VIP reservations and stale-writer guards are complete. Instructions,
receipts and consumer/producer integration remain.

Earlier completed fix: **Retry unavailable leaders during confirmed lease cleanup**.
The client retries HTTP 503 during its initial DELETE and subsequent confirmation
GETs, within the same 30-second deadline. HTTP 202 remains only acceptance;
completion requires DELETE 204 or GET 404 after acceptance. Permanent errors
still return immediately. No compatibility change (protocol 16/state 29).

Two HTTP contracts fail first (0.098s; the permanent-refusal contract already
passed). Native/Linux pass all 41 client/lease cases (1.216s/1.735s), the actual
three-node capacity-refusal regression (22.188s/21.981s), strict all-target,
all-feature Clippy and formatting. Logs: `/tmp/rb-lease-retry-{red,native,linux}.log`.
Both pipelines are reaped; no Rust compilation is active.

Next: committed remote withdrawal obligations, authenticated consumer receipts
and producer release gates from the OCI plan. Then actual OCI interruption/reboot
qualification and production selection, followed by V01–V04. C34 remains open.
The older hosted run for 8bd5b65 finished with six successful jobs, the one known
multi-node failure and six jobs cancelled when the next commit was pushed. New
commits need their own hosted checks; do not call that older run green. No merge,
tag, candidate or release has been published.

Earlier completed feature: **Bound runtime inventory reads across caller cancellation**.
A shared InventoryReader retains its semaphore permit inside the surviving read
task, so a cancelled report cannot admit overlapping filesystem workers. Process
clones share the reader; owned Runc stores it in shared Ownership, not the fresh
journal created per poll. Cancelled queued reads never run later; failed reads
release admission for retry. No format change (16/29).

The cancellation contract fails first (one failed/one passed, 0.088s), then both
pass (0.065s). Native/Linux: 477/522 affected library cases (18.306s/29.261s),
29/30 process recovery cases (20.322s/20.851s), real Runc preparation (1.21s),
retained-address recovery/release (1.85s), strict Clippy and formatting. All
pipelines are reaped. Logs: `/tmp/rb-inventory-reader-{red,focused,native,linux}.log`.

The hosted multi-node failure found at this checkpoint is fixed and qualified
above. Its original log remains `/tmp/rb-ci-multinode-8bd5b65.log` (job
106553973514, workflow 35666641207). The source contains the new HTTP contracts;
the proposed `/tmp/rb-lease-retry-tests.txt` copy is no longer a pending patch.
The next C34 work is committed remote withdrawal and release authorisation.

Earlier completed feature: **Preserve original execution identity through cluster discovery**.
Bun matches runtime inventory against the canonical instance and full spec, the
worker and leader preserve the identity, and remote routing distinguishes a new
execution on the same port. Missing, duplicated or mismatched evidence withholds
the identity without hiding capacity. Fingerprint deserialisation rejects invalid
values. Current formats are protocol 16/state 29; fresh development clusters are
required. All local pipelines are finished and no Rust builds are active.

Five contracts fail first (0.101s), then pass (0.052s). Final native/Linux:
688/700 affected library cases (18.475s/32.504s), 14 integration cases each
(5.274s/5.236s), three real placement cases (36.718s/38.653s), strict Clippy and
formatting. Seven ignored integration cases were skipped; the actual TCP case
verifies a non-empty execution identity. Logs: /tmp/rb-report-execution-{red,
focused,native,linux}.log. Initial compile-only attempts corrected the test worker
constructor name and fully qualified Duration/HashMap before the executed runs.

The inventory-reader follow-up is now complete; see the current checkpoint above.
The remaining protocol work is committed withdrawal obligations/consumer receipts
and producer release gates from the OCI plan. C34 and V01–V04 remain open.

Earlier completed runtime feature: **Expose non-secret original runtime generation fingerprints**.
Runtime inventory now carries a stable fingerprint of the original execution;
recreation changes it, and discovery rejects a fingerprint from another Runc
address owner. Three contracts fail first (0.124s). Qualification passes:
473 native/518 Linux affected library cases (18.947s/31.944s), 29/30 process
recovery cases (20.313s/20.852s), real Runc preparation/recreation (1.31s) and
retained-address recovery/release (2.10s), and strict checks on both platforms.
All pipelines are finished. Logs: `/tmp/rb-runtime-generation-contract-red.log`
and `/tmp/rb-runtime-generation-{native,linux}.log`. An earlier compile-only
attempt needed a test comparison through JSON because DiscoveryInventory does
not implement PartialEq; the red log above contains three executed failures.

Next: carry the canonical instance ID and non-secret runtime generation through
reports and catalogue entries, so remote routing distinguishes successive owners
of the same host port. The fingerprint is currently in-memory runtime evidence;
no wire/persistent format changed (15/28). Then implement the committed withdrawal
ledger, authenticated consumer receipts and producer release gates as described
in the OCI plan. Never publish Process owner nonces.

Earlier completed feature: **Persist catalogue consumers before serving placements**.
Consumer registration, snapshot retention, bounded admission and explicit
operator decommission are wired. Credential-free development clusters still
register consumers; configured credentials require the internal service principal
and TLS peers must match their node identity. This does not yet permit address
reuse. Catalogue publication also now compares against committed state.

All local pipelines finished: 426 affected tests, ten agent/compatibility cases,
three real placement cases, two leader-failover/decommission cases, and strict
Clippy/formatting pass on macOS and Linux. Exact evidence and earlier failed
attempts are in the latest continuation below. Current formats: protocol 16/state
29, OCI intent 4, job checkpoint 2, lease 5. Hosted CI needs current-head inspection.

Next: exact original endpoint exposure identities, then committed withdrawal
obligations/consumer receipts and producer release gates. The active OCI plan's
"Remote catalogue retirement continuation" gives the sequence. Process owner
nonces are control capabilities: never publish them; use a separate non-secret
identity. Standalone recovery and retirement are already implemented.

C34 also still needs actual Bun death/caller cancellation around OCI
init/start/adopt/retire/retry, host reboot evidence, and production selection of
owned Runc/persistent kernel paths. Then V01–V04. No merge, tag or release yet.

## Latest continuation: exact service snapshot restoration

The commit titled **Restore exact service allocations from validated snapshots**
adds `ServiceMap::from_snapshot`. It preserves original collision-resolved VIPs,
destination identities, ports, firewall configuration and backend evidence,
rebuilds address reservations, and rejects an invalid or conflicting inventory
as a whole. Three regressions fail against ordinary registration replay. All
106 macOS/118 Linux discovery library tests pass (0.195s/0.183s), with strict
all-target/all-feature Clippy and formatting on both platforms.

This is a recovery primitive only. The checkpoint store described below adds
persistence, but no production caller, runtime reference correlation or
publication gate is wired yet. Use this constructor to validate and restore
exact allocations before any recovered routing publication. Saved
health is historical, not permission to serve a recovered backend. The C34
parent and V01–V04 remain open; no compatibility format changed in this step.

Detailed local logs, if retained: `/tmp/rb-discovery-snapshot-red.log`,
`/tmp/rb-discovery-snapshot-native.log`, `/tmp/rb-discovery-snapshot-linux.log`.
Hosted `51b7594` passed all 19 checks before this continuation; the new push needs
its own hosted qualification.

## Latest continuation: durable discovery checkpoint

`src/bun/discovery_owners.rs` implements exclusive claiming, complete validated
inventories and transitions, private atomic publication and failed-write fencing.
All seven initial contracts failed first. Review reproduced an eighth failure:
a missing claim was recreated on the first failed open, then trusted on retry.
Only a newly created directory now permits initialisation; repeated recovery
with missing established ownership keeps refusing.

Initial qualification passed 113 macOS/125 Linux library tests, eleven runtime
intent integrations on each platform, and strict Clippy/formatting on both.
Final qualification after the missing-claim fix passes 114 macOS/126 Linux
discovery tests (0.268s/0.623s), strict all-target/all-feature Clippy and formatting
on both platforms. Logs:
`/tmp/rb-discovery-journal-final-{native,linux}.log`; red tests are in
`/tmp/rb-discovery-journal-red.log` and `/tmp/rb-discovery-journal-claim-red.log`.
Both compilation pipelines have completed. The commit titled **Persist exclusive
discovery ownership and cleanup permissions** includes the store, tests, book
and progress updates. Its hosted checks must be inspected separately.

**The agent is unchanged. Resume with Bun integration.** Keep the journal in a blocking worker that retains its exclusive
claim across cancelled callers. Correlate original runtime inventory, preserve
attempted publication before kernel/userspace writes, and durably authorise
release only after complete withdrawal proof. Missing state must never become
an empty service map. Schema 1 is local to this unused store; no production
compatibility format changed. C34 and V01–V04 remain open.

## Latest continuation: checked replacement publication

The commit titled **Confirm replacement backend publication before rollout cutover**
fixes premature userspace routing after a kernel publication refusal. The actual
frozen-map regression fails first (8.72s). A candidate service map is now validated
and published to the kernel before DNS/Wrapper. Checked channel results send
rolling and blue-green failures through their existing abort/retained-cleanup path.
Closed agent channels and missing publication inputs refuse.

Real rolling refusal/success pass (18.78s), blue-green refusal passes (9.52s),
and all 217 affected library cases pass on macOS/Linux (17.663s/24.082s), with
strict Clippy and formatting. Both compilation pipelines have completed. Logs:
`/tmp/rb-rollout-publication-{red,fixed-linux,final-native,final-linux}.log`.

The generation-adoption fixture previously combined a portless original spec,
an allocated host port and a service redeploy without recovered discovery. It
now tests consistent portless generation continuity. Do not recreate guessed
service allocations to bypass refusal. Service adoption needs original discovery
reconstruction. Health updates and durable journal integration remain open.

The cancellation-safe blocking journal path below is now implemented for Bun integration:
filesystem work must leave the async runtime, retain the exclusive claim after
caller cancellation, and require recovered state after an unacknowledged write.

## Latest continuation: cancellation-safe discovery writes

The commit titled **Keep discovery claims through cancelled blocking writes** adds
`DiscoveryJournal::open_async` and consuming `persist(self, next)`. Filesystem work
runs on blocking workers; successful acknowledgement returns the journal. Failure
or cancellation requires reopening and reconciling durable state. The worker keeps
the exclusive claim until completion even if the waiting future is cancelled.

Both stalled-storage contracts fail against inline writes (2.122s). All ten
checkpoint tests now pass on macOS/Linux (0.295s/0.162s), with strict Clippy and
formatting. All pipelines are complete. Logs:
`/tmp/rb-discovery-worker-{red,native,linux}.log`. Bun integration remains open.
The caller must distinguish unopened ownership from a journal consumed by an
unacknowledged write; neither cancellation nor a missing handle proves empty state.

## Latest continuation: checked initial service registration

The commit titled **Refuse workload launch after initial service publication fails**
propagates `RegisterServiceApp` map and kernel publication errors through the
worker channel. A fresh deployment stops before create/start; logical/service
allocations stay owned while cleanup is uncertain. A closed channel also refuses.
The restart driver cannot launch untouched fresh Pending instances.

The strengthened real frozen-map regression first observes a Running process and
adoption record despite failed initial publication (0.27s). It now confirms no
runtime or adoption record (5.25s). All 218 affected tests pass on macOS/Linux
(17.688s/24.119s), with strict Clippy and formatting. All pipelines completed.
Logs: `/tmp/rb-registration-{red,native,fixed-linux}.log`.

Next: health publication must retry kernel refusal, publish confirmed userspace
health and prevent a premature restart. Fresh/final rollout backend insertion
errors must also propagate. Complete durable journal integration, remote/drain
acknowledgements and the remaining C34/V01–V04 gates still follow.

## Latest continuation: checked health publication

The commit titled **Confirm health withdrawal before automatic restart** fixes
both stale userspace health and premature restart after kernel refusal. The
corrected subscribed-view regression fails first (0.102s), as does the real
frozen-map process case (3.32s, Pending/restart count one). Ignore the first
native attempt for this scope; its late subscription exposed a separate empty
watch-snapshot issue now recorded in progress.

Bun confirms a candidate kernel health view before userspace publication or
restart. Later probes retry even when lifecycle health already changed. Portless
workloads skip backend mutation. Runtime/adoption evidence stays owned on refusal.
The real case passes (5.61s); 263 affected tests pass on macOS/Linux
(17.764s/23.956s), followed by the positive retry contract (0.041s/0.034s).
Strict Clippy/formatting pass on both. All pipelines are complete. Logs:
`/tmp/rb-health-publication-red-native-confirmed.log`,
`/tmp/rb-health-publication-red-linux.log`,
`/tmp/rb-health-publication-native.log`,
`/tmp/rb-health-publication-fixed-linux.log`,
`/tmp/rb-health-retry-{native,linux}.log`.

Next: checked fresh/final/restart backend insertion and correct replacement at
backend capacity; preserve the last watch snapshot without subscribers. Complete
journal integration, remote/drain acknowledgement and C34/V01–V04 still follow.

## Latest continuation: replacement at backend capacity

The commit titled **Allow backend replacement at full capacity** moves the existing
instance check before the insertion limit. Its regression fails first (0.087s),
then verifies replacement of address/port/health at 32 entries and refusal of a new
endpoint without mutation. All 107 native Onion tests pass (0.213s), with strict
Clippy/formatting. No compilation is pending. This is portable map logic; Linux
coverage comes from later affected qualification and hosted CI. Logs:
`/tmp/rb-backend-capacity-{red,native}.log`.

Next: propagate fresh/final/restart backend insertion errors rather than reporting
successful deployment with missing endpoints; preserve the latest watch snapshot
without readers. Complete journal integration and C34/V01–V04 still follow.

## Latest continuation: checked backend insertion

The commit titled **Fail deployment when backend insertion is refused** propagates
fresh/final rollout insertion and final registration errors. Restart insertion
failure uses the existing failed-restart path. The 33-endpoint regression fails
first with a false Complete (0.265s), then verifies failure and retention of every
created runtime's cleanup owner.

All 275 native/287 Linux affected agent and Onion tests pass
(17.695s/24.086s), with strict Clippy/formatting on both. This also qualifies the
preceding capacity fix on Linux. All pipelines are complete. Logs:
`/tmp/rb-backend-insertion-{red,native,linux}.log`.

Next: preserve the latest service-map watch snapshot without subscribers. Then
complete journal integration, remote/drain acknowledgements and C34/V01–V04.

## Latest continuation: latest snapshot without subscribers

The commit titled **Retain discovery snapshots for late subscribers** uses
`send_replace` so a channel with no readers still retains a completed deployment.
The late-subscriber regression fails first (0.101s), then passes with both health
publication/retry cases (three native tests, 0.049s). Strict Clippy/formatting pass.
No compilation remains pending. Logs: `/tmp/rb-discovery-watch-{red,native}.log`.

The live publication fixes are complete at their recorded scopes. Next is actual
Bun journal integration: capture complete original publication obligations before
writes, reconcile them against runtime/kernel inventory before adoption or cleanup,
and durably authorise release only after withdrawal proof. Remote/drain completion,
actual Bun-death/reboot qualification, production selection and V01–V04 remain open.

## Read these first

- [Progress ledger](../progress.md): authoritative checked/unchecked completion
  items. Historical counts describe their checkpoints, not today's complete suite.
- [OCI ownership plan](2026-09-20-oci-launch-ownership.md): the active C34 work,
  physical failures, fixes and next discovery transaction boundaries.
- [Foreground process ownership](2026-09-20-foreground-process-ownership.md):
  implemented production supervision and remaining interruption qualification.
- [Completion plan](2026-09-17-codebase-completion-plan.md): all C/H/F/V dispositions.
- [Release plan](2026-09-16-v0.1.0-release-plan.md) and
  [release procedure](../releasing.md): supported scope, acceptance and candidate
  promotion. Optional H05 and deferred F01–F12 are not automatically release work.

## Decisions already made

These do not need another clarification:

- Pre-release development clusters require fresh state. Later upgrades require
  explicit compatible protocol/state formats. Current formats are protocol 16,
  state 29, OCI intent schema 4, job checkpoint schema 2 and lease schema 5.
- Process workloads must stay in the foreground and retain their children in
  the supervised process group. Detached/daemonising workloads use Linux containers.
- macOS containers use managed Linux VMs. Direct Apple Container selection is
  disabled for 0.1.0; its daemon-operation recovery is deferred under F10. Native
  foreground process mode remains available.
- Unknown ordinary jobs require an explicit rerun. Cron skips missed or uncertain
  firings after a crash; bounded job retries are a separate policy.
- Cleanup needs confirmation. An operator may discharge a disconnected node's
  obligations only after externally stopping/isolating it and permanently
  retiring its identity. Return requires fresh enrolment under a new identity.
- The original development signing key was unavailable. Its replacement public
  identity is committed and the matching `RELIABURGER_RELEASE_KEY` Actions secret
  was configured with explicit user approval. Do not regenerate it or expose the
  private key. No candidate, release tag or public release has been published.

## What is implemented

The ledger contains the full history. These distinctions matter when resuming:

- Production foreground ProcessGrill uses durable generation owners, launch
  inventory, authenticated control and positive descendant retirement. Job/cron
  checkpoints, initialiser ownership and auxiliary exec supervision are wired.
- Owned rootful/rootless Runc adapters exist and have physical recovery tests.
  They retain original specifications, lifecycle claims, command inventories,
  launchers and network helpers. **They remain opt-in.** Production Runc selection
  awaits complete discovery recovery and qualification.
- The persistent kernel loader retains and verifies original maps/links across
  loader death. **Production loading is still ephemeral.** Actual Bun death and
  host reboot remain separate acceptance boundaries.
- Original source/egress ownership is durable. Namespace policy covers jobs and
  portless workloads; destination identities use allocated VIPs. Explicit service
  and rollout retirement check backend withdrawal, destination-grant deletion and
  kernel errors rather than reporting false completion.
- Registry/storage ownership, lease cleanup, permanent node decommission,
  credential renewal, compatibility checks, signed-candidate preservation and
  managed installation are implemented with scoped evidence in the ledger.

Recent commits, oldest first:

| Commit | Completed item |
| --- | --- |
| `4ecbfa5` | Final kernel backend publication errors propagate instead of reporting a completed deployment. |
| `2e0563b` | macOS uses managed Linux VMs; direct Apple Container is excluded from 0.1.0. |
| `9b925a5` | Local catalogue endpoints are excluded using configured node identity, even without council metrics. |
| `d50d320` | Destination grants must positively retire before service identity reuse. |
| `e2f7131` | Actual Bun death during foreground initialisation is qualified; explicit retry starts a fresh chain. |
| `625fb17` | Lost cancellation requests retry within the original deadline; only positive exit evidence completes command retirement. |
| `2b01c6a` | Original generation-bound network references retain addresses through natural exit and adapter recovery; stale releases refuse. |
| `ca0318b` | Peer-route qualification uses private network/mount namespaces and preserves the host's firewall. |
| `51b2ee8` | Lost enforcement stops owned runtime execution even when backend withdrawal refuses; records and address holds remain. |
| `f4ddf62` | Exact service snapshot recovery preserves collision-resolved allocations and validates the complete inventory. |

The address-hold regression originally let an old VIP return HTTP 200 from an
unrelated portless successor after its predecessor exited. The combined-loss
regression originally left the payload running without enforcement for the
entire 15-second observation deadline. Both now pass against real Runc and
kernel maps. Controller-task loss is not actual Bun process death.

## Latest evidence

| Scope | Result |
| --- | --- |
| Linux real kernel suite after `51b2ee8` | 68 passed, 82.57s |
| Focused combined enforcement-loss/backend-refusal case | Passed, 11.97s; failed before repair, 16.58s including cleanup |
| Affected agent/firewall/egress library cases after `51b2ee8` | 215 passed on macOS, 17.738s; 215 on Linux, 25.437s |
| Strict all-target/all-feature Clippy and formatting | Passed on macOS and Linux after `51b2ee8` |
| Owned rootful/rootless runtime checkpoint at `2b01c6a` | 12 rootful passed, 11.65s; six rootless passed, 3.33s |
| Command/journal/compatibility checkpoint at `2b01c6a` | 31 passed on each platform; deterministic cancellation reset/deadline tests fail before repair |
| Network fixture at `ca0318b` | Both ACCEPT and DROP outer-policy probes pass, unchanged policy; four privileged cases pass, 1.19s; two refusal contracts pass |
| Hosted `ca0318b` | 19 successful checks, none failed/running; candidate-only jobs skipped on the PR |

Hosted `e2f7131` had one privileged peer-route failure (107 passed). A DROP
forwarding policy reproduces the same packet loss locally. Its exact hosted
blocking rule was not captured. The isolated fixture passes, and hosted
`ca0318b` is green. Do not change the operator's host firewall to make tests pass.
Direct Linux installations must permit the required forwarding.

A complete hosted run of the newest head is still distinct from the local
results above. Native qualification used Rust 1.98; the test VM used Rust 1.97.

Temporary detailed logs, if still present on this laptop:

- `/tmp/rb-retirement-reset-red.log`
- `/tmp/rb-network-reference-retirement-native.log`
- `/tmp/rb-network-reference-retirement-linux.log`
- `/tmp/rb-peer-route-policy-probe.log`
- `/tmp/rb-peer-route-isolation-linux.log`
- `/tmp/rb-combined-policy-red-linux.log`
- `/tmp/rb-combined-policy-fixed-linux.log`
- `/tmp/rb-combined-policy-native.log`
- `/tmp/rb-combined-policy-suite-linux.log`

The committed evidence above is sufficient to understand the checkpoint if
`/tmp` has been cleared. Preserve existing qualification outputs and stashes.

## What remains, in order

1. **Integrate durable discovery ownership and reconstruction (C34).** The
   snapshot restorer and checkpoint store are implemented. Wire them to persist
   exact allocated VIPs/ports, destination grants, attempted backend publication
   and original runtime references before publishing. Validate the complete
   inventory before adoption/cleanup. Preserve collision-resolved allocations;
   never infer cleanup from an empty in-memory map. Cover checkpoint failures,
   frozen maps, partial publication and repeated recovery. Finish checked
   intermediate health/replacement updates as part of this integration.
2. **Prove remote and in-flight withdrawal (C34).** A local kernel update does
   not prove another node's catalogue stopped advertising an endpoint or that
   Wrapper finished requests holding an old backend. Confirm those obligations
   before reusing addresses, host ports or service identities. Apply the approved
   permanently fenced decommission contract when a node cannot confirm.
3. **Finish real interruption/reboot qualification (C34).** Exercise actual Bun
   death and cancellation before/after init preparation, activation, adoption,
   retirement and application retry. The foreground running-initialiser case is
   done; that does not cover every boundary or OCI execution. Distinguish a host
   reboot using positive boot identity and runtime absence, rather than treating
   missing pins, sockets or PIDs as successful cleanup.
4. **Wire production owned Runc and persistent kernel loading (C34).** Only after
   complete reconciliation passes; rerun actual Bun-death and signed upgrade/
   rollback boundaries on that production path. Keep broad parent checkboxes
   open while this integration or required qualification remains unfinished.
5. **V01:** Run the complete live catalogue/chaos/diagnostic matrix on independent
   Runc nodes, including client death, leader change, volumes and cleanup proof.
6. **V02:** Complete sustained certificate, storage, restart and upgrade recovery
   qualification against the final source/candidate, rather than relying on a
   short successful smoke test.
7. **V03:** After independent correctness work is complete and main contains the
   workflow, build the exact signed candidate, stage it through HTTPS and install
   those bytes. Promotion must not rebuild them. Review/merge is a later operator
   step, not permission to skip qualification.
8. **V04:** Repeat cold installs on the advertised host matrix, measure the
   under-five-minute experience and retain failure/cleanup evidence. This used
   test VM is not a cold host. Intel macOS still needs cold-install qualification.

V05's dependency review is complete for the current lockfile, with dated
exceptions. Do not silently expand supported features or substitute library
coverage for a required physical gate.

## Latest continuation: restart routing and health

The commit titled **Publish confirmed restart addresses with readiness checks**
adds two failing-first regressions (0.099s): a normal exit/restart left DNS on the
old container IP, and a health-triggered restart published a healthy backend
while still in HealthWait. Restart now builds a candidate map, confirms kernel
publication, then refreshes the shared map/routing view. A health-checked backend
stays unhealthy until its probe succeeds. Refused publication preserves the
previous view and retains the failed runtime for cleanup.

All 171 affected native/Linux agent tests pass (17.763s/23.954s). Strict
all-target/all-feature Clippy and formatting pass on both platforms.
The initial native suite was sandbox-denied local health sockets; rerunning with
socket access passes. Logs: `/tmp/rb-restart-discovery-red.log`,
`/tmp/rb-restart-discovery-native-qualified.log`, and
`/tmp/rb-restart-discovery-linux.log`. No compatibility format changed. C34,
remote/captured-request withdrawal and V01–V04 remain open.

## Latest continuation: nonblocking drain notifications

The commit titled **Keep drain cleanup responsive under notification backpressure**
replaces awaited sends under the tracker lock with `try_send`. A full queue
retains the completion for retry; a closed receiver still allows polling callers
to observe completion. The full-queue regression fails first (0.192s). All 96
native Wrapper tests pass (0.491s), with strict all-target/all-feature Clippy and
formatting. This portable channel change awaits the next Linux suite/hosted run;
no physical networking semantics changed. Logs: `/tmp/rb-drain-queue-red.log` and
`/tmp/rb-drain-queue-native.log`. Request capture and positive drain completion
remain open.

## Latest continuation: captured ingress ownership

The commit titled **Retain captured ingress ownership until requests release**
passes qualification. Route selection and candidate accounting now
share the routing read lock. Withdrawal cannot pass an unaccounted capture.
Starting a drain preserves existing counts; normal inactive entries are removed
after their last guard drops. All failover candidates remain conservatively held;
WebSockets capture one candidate. Deadline cancellation no longer reports drain
completion until both counts reach zero.

HTTP request bodies, upstream connects/headers, response streams and WebSocket
handshakes/splices observe cancellation. A bounded one-item response pump keeps
cancellation responsive when the downstream stops polling. tokio-util's `rt`
feature supplies `AbortOnDropHandle`; dropping a response aborts its pump. The
connection permit stays with the response, backend ownership with the pump.

Three regressions fail first (0.101s): request predating drain, captured failover,
and deadline mistaken for release. The initial 271 native Wrapper/agent tests
pass (17.801s); all 101 final native Wrapper tests pass (0.517s), including upstream
header cancellation, a live WebSocket and an unpolled response. Native strict
Clippy/formatting pass. All 272 Linux Wrapper/agent tests pass (24.159s),
with strict Clippy/formatting. Both pipelines have completed. Logs:
`/tmp/rb-ingress-ownership-red.log`, `...-native.log`, `...-final-native.log`,
`...-linux.log` (same prefix). No compatibility format changed.

**Next integration:** these local guards govern deployment drains. Audit ordinary
stop, natural-exit cleanup, retries and recovered retirement: each must withdraw
routes and require guard release before allowing address/host-port/service reuse.
The current automatic-restart path still needs this pre-retirement drain proof.
Remote catalogue acknowledgement and durable journal reconstruction remain open;
a local empty map is not evidence of either. Do not mark C34 complete.

## Latest continuation: ordinary stop and retry ingress gates

Both failing-first regressions reproduce premature runtime retirement (0.099s).
`stop_and_wait_for_exit` now uses the deployment drain-and-stop helper after the
existing routing withdrawal/supervision fence. Pending automatic retries and
failed-restart cleanup call `poll_restart_withdrawal`: withdraw, start the drain,
sweep, and retain Pending/Stopping until guards release. The retry tick never
waits for the drain deadline. Tests require no kill while captured references
remain and successful restart on a later tick after release.

The commit titled **Drain captured requests before stop and automatic retry**
passes all 274 native/Linux agent and Wrapper tests (17.763s/26.645s), with
strict all-target/all-feature Clippy and formatting on both.
Logs: `/tmp/rb-retirement-drain-red.log`, `...-native.log`, `...-linux.log`
(same prefix). Both pipelines have finished.
Remaining: recovered/natural artifact cleanup and durable/remote
withdrawal proof before allocation reuse; production owned-Runc selection and
all previously listed acceptance gates.

## Latest continuation: cancelled HTTP response errors

The real partial-response regression fails first (0.126s): the client receives
seven bytes of a promised 100, then cancellation appears as successful EOF.
The response pump now returns a typed result. After the chunk channel closes,
the response stream awaits that result: cancellation becomes ConnectionAborted,
a failed task becomes a body error, and normal completion remains EOF. Backend
guard release stays independent of downstream polling.

All 102 native/Linux Wrapper tests pass (0.514s/0.513s). Strict linting caught
an unchecked read amount in the new fixture; it now reads the complete request
header. Final focused reruns pass (0.066s native/0.044s Linux), with strict
all-target/all-feature Clippy and formatting on both. Both pipelines finished. Logs
`/tmp/rb-drain-stream-error-red.log`,
`...-native.log`, `...-linux.log`, and `...-final-{native,linux}.log`
(same prefix). The commit is titled **Report drain cancellation as an HTTP body error**.
Resume the natural/recovered artifact cleanup ingress gate,
then durable discovery and remote acknowledgement work. PR description is
current through `29421be`; hosted checks for later commits still need inspection.

## Latest continuation: artifact cleanup ingress gate

The stopped-runtime regression fails first (0.119s): cleanup deletes adoption
and identity files while a captured request remains. `retire_instance_artifacts`
now polls local withdrawal before policy clearing, runtime-reference release and
file deletion. It requests immediate cancellation for an absent runtime, refuses
while guards remain, and succeeds on retry after release. The common helper is
now `poll_instance_withdrawal`; automatic retries pass their normal grace period.

All 174 native/Linux agent tests pass (17.701s/24.306s), with strict
all-target/all-feature Clippy and formatting on both. Both pipelines finished. Logs:
`/tmp/rb-artifact-ingress-red.log`, `...-native.log`, `...-linux.log` (same prefix).
The commit is titled **Retain stopped-runtime artifacts until ingress releases**.
The next core work is actual Bun integration of
`DiscoveryJournal`, followed by remote acknowledgement and the recorded real
crash/reboot/production-selection gates. Local guards do not supply any of those.

## Latest continuation: Bun durable-publication integration

The commit titled **Journal Bun publication before acknowledging routing**
adds `src/bun/agent/discovery_ownership.rs`, a Disabled/Ready/Uncertain
ownership state and an opt-in fresh-agent opener. Actual Bun publication now
persists a conservative inventory before kernel updates; only acknowledged writes
restore Ready. Absent services remain owned in the checkpoint. A failed checkpoint
prevents initial runtime create/start and fences later publication even after the
path is repaired. Existing journals are refused by the fresh opener; adoption
refuses existing records or runtime launches before runtime recovery can mutate
anything. This is not production selection or completed recovery.

Three agent regressions fail first (0.147s): missing durable publication, launch
after failed persistence, and adoption without original discovery evidence. An
additional case retains an earlier service through publication of a different one.
The test-only journal pause receiver now uses Tokio oneshot so storing it in Bun
preserves Sync; blocking_recv remains confined to the blocking writer.

All 188 native/Linux agent and journal tests pass (17.817s/22.813s), with
strict all-target/all-feature Clippy and formatting on both. Both pipelines
finished. Logs `/tmp/rb-agent-discovery-red-confirmed.log`,
`...-native.log`, `...-linux.log` (same prefix); initial compile diagnostics are in
`/tmp/rb-agent-discovery-red.log`.

Next: complete original runtime-reference capture/correlation, recovery of exact
allocations without publishing historical health, durable retirement/release
authorisation, and remote withdrawal proof. The fresh-only opt-in producer must
remain out of production selection until these consumers and crash tests pass.
Compatibility is unchanged. Do not mark C34 complete.

## Latest continuation: durable original runtime references

Two regressions fail first (0.162s): runtime launch has no saved original reference,
and a checkpoint failure after initial service registration still permits Start.
The commit titled **Persist original discovery references before runtime launch**
journals exact ServiceId/NetworkReference/Held before Start,
using the same consuming update helper as publication. Original instance identity
is checked, failed writes retain the runtime hold, and enabled discovery refuses
physical release without an exact ReleaseAuthorised entry. A third regression
checks that release fence. MockGrill exposes a configurable original reference
through an async mutex; it does not implement successful release yet.

A new real Linux case, `durable_discovery_retains_original_reference_after_controller_loss`,
extends the isolated owned-Runc/eBPF address-retention fixture: after controller-task
abort it reopens the journal, compares exact generation/index, then checks that
natural exit cannot redirect the old VIP to a successor. This is not actual Bun
process death and must not be counted as that gate.

All 191 native/Linux agent, journal and mock tests pass (17.820s/23.205s).
The real owned-Runc/eBPF case passes (10.67s). Strict all-target/all-feature
Clippy and formatting pass on both; both pipelines finished. Logs are `/tmp/rb-discovery-reference-native.log`
and `/tmp/rb-discovery-reference-linux.log`.
Red log: `/tmp/rb-discovery-reference-red.log`.

Next: authorised withdrawal/release checkpoints and full original-inventory
recovery, including the crash between runtime hold and its journal acknowledgement.
For a standalone agent, local kernel withdrawal plus captured-request release can
authorise an exact reference; a clustered agent must retain it until remote proof
is implemented. Persist ReleaseAuthorised before the runtime release call, then
forget only after acknowledgement. Service retirement similarly needs durable
Withdrawn before forgetting its original allocation. Do not authorise cluster
cleanup using the standalone path.
Do not infer authority from a missing entry. Remote catalogue acknowledgement is
still required before enabling cluster production selection. Compatibility and
production runtime/kernel selection remain unchanged.

## Completed: standalone durable release permission

The ordering regression fails first (0.158s): standalone Stop cannot complete
because no ReleaseAuthorised transition is wired. The implementation checks
the original service allocation and absent backend, persists an exact permission,
calls runtime release, then forgets the reference after acknowledgement. It only
authorises this locally when `cluster.is_none()`; clustered agents refuse until
remote proof is implemented. A broken checkpoint must prevent the runtime call.
MockGrill now supports generation-checked release and a deterministic release gate
so the test can inspect the durable checkpoint before physical acknowledgement.

All 193 native agent/journal/mock tests pass (17.860s), with strict checks.
The real Linux `standalone_discovery_release_allows_confirmed_address_reuse` case
passes (26.28s), confirming actual address reuse without stale VIP reachability.
All 193 Linux library cases pass (22.919s), with strict Clippy and formatting. Both pipelines finished. Logs `/tmp/rb-discovery-release-red-confirmed.log`, `...-native.log`, and
`...-linux.log` (same prefix). The first red script failed before editing and ran
zero tests; that earlier `...-red.log` is not behavioural evidence. This item is committed separately. PR description is current through `f536668`.

Next: durable service Withdrawn/forget transitions; automatic application retry
must retire its original held address before same-id successor creation (its
specialised artifact cleanup currently retains that reference). Then full
original-inventory recovery, including replay of ReleaseAuthorised after lost
acknowledgement, and remote catalogue proof. Production selection is unchanged.

## Completed: confirmed standalone service retirement

Three regressions fail first (0.198s): successful Stop leaves a durable Owned
service, failed checkpoint still frees its VIP, and clustered Stop without a
runtime reference forgets an unconfirmed allocation. The implementation
persists Withdrawn and then forgets the original service before unregistering
its VIP, after exact-allocation, runtime-reference and ingress checks. Clustered
retirement still refuses without remote proof. The physical address-reuse case
now also requires an empty service inventory.

All 196 affected native/Linux tests pass (17.996s/24.243s). Strict Clippy/formatting pass on both platforms. The real Linux
address-reuse/service-retirement case passes (25.91s). Both pipelines have finished.
Logs `/tmp/rb-service-retirement-red.log`, `...-native.log`, `...-linux.log`.
This item is committed separately. Next: automatic application retry must release
its original retained address before same-id successor creation, then complete
original-inventory recovery and remote acknowledgement.

## Completed: automatic-retry address retirement

Both ordering/persistence regressions fail first (0.199s). The specialised
application retry cleanup now releases the original network reference after
policy removal and before deleting execution evidence or creating a successor.
It preserves the logical workload identity bundle/mount. The new physical case
kills the original container, waits for a changed runtime generation, verifies
the exact replacement journal hold and original VIP reachability, then uses the
existing retirement/reuse checks. Actual Bun death during retry is still open.

All 198 affected native/Linux tests pass (18.005s/22.924s), with strict
Clippy/formatting on both. The real Linux automatic-restart case passes (28.05s).
Both pipelines have finished.
Logs `/tmp/rb-retry-address-red.log`, `...-native.log`, `...-linux.log`.
This item is committed separately. PR description is current through `3943b8f`.
Next: original-inventory recovery/correlation and remote catalogue acknowledgement.

## Completed: original runtime/discovery correlation

Three recovery contracts fail first (0.140s); the existing release-permission
preservation case already passed. RuntimeLaunch now carries the original Held
or Released reference from the same Runc intent (Process returns None). The
journal's pure `reconcile_runtime_inventory` validates exact references, requires
permission for released runtime evidence, recovers an unacknowledged hold only
from its original service/cgroup/port, and refuses duplicate owners or published
backends without original holds. It returns a candidate, never mutates ownership.
This is an owned-container recovery boundary; it does not establish remote proof.

The physical retained-address case now asserts that complete runtime inventory
preserves both Held and Released evidence. All 233 affected native/Linux tests pass (18.083s/24.113s), with strict
Clippy/formatting. The real retained-address inventory case passes (1.99s).
Both pipelines finished. Logs have prefix `/tmp/rb-discovery-correlation-`.
This item is committed separately.
Before integration, fix `BpfServiceMap::read_backends`: `.ok()` currently turns
all lookup failures into absence. Only `MapError::KeyNotFound` proves absence;
other errors must block retirement. A subprocess-scoped seccomp denial can
exercise the real syscall failure without poisoning other physical tests.
Then persist the reconciled candidate, restore exact allocations without
publishing historical health, withdraw original routing, replay pending release
permissions and reconcile adoption/cleanup before enabling production recovery.

## Completed: kernel backend lookup errors

The physical subprocess-scoped seccomp regression reproduces a denied BPF lookup
being returned as absent (0.12s). `read_backends` now returns None only for
MapError::KeyNotFound and propagates every other error. The subprocess first
proves normal absence, then denies only BPF calls on its own test thread.

Native strict checks pass; all 73 physical Linux kernel cases pass (157.25s).
All 322 affected Linux library tests pass (24.176s), with strict Clippy and
formatting. Both pipelines finished. Logs `/tmp/rb-backend-read-red.log`, `...-native.log`, `...-linux.log`.
This item is committed separately. PR description is current through `aafc34c`.
Next: agent integration of recovered original inventory, retaining allocations
without publishing historical health; confirmed withdrawal and idempotent replay
before adoption/cleanup, then remote acknowledgements and actual Bun/host crash
qualification. C34 and V01–V04 remain open.

### Recovery integration design at `ad39b3b`

These notes describe the gap at `ad39b3b`; the in-progress implementation below
now addresses it, with qualification still running. At that commit the pure
correlator was not yet called by Bun recovery. The fresh-only opener and
`require_discovery_recovery` still refuse established ownership. A recovery
transaction must hold the journal claim, require complete runtime inventory,
persist correlated references, reserve exact original VIPs with historical
backends unpublished, and confirm withdrawal using those original keys. Unknown
kernel keys/owners must refuse before any cleanup. Do not interpret historical
health as permission to publish.

`adopt_recorded_instances` checks recovery twice (before record validation and
after reading runtime launches); arrange the new transaction before any runtime
mutation. `reconcile_runtime_launches` retires unrecorded runtimes before calling
artifact cleanup. Those instances have no supervisor entry, so
`withdraw_instance_backend` currently returns early: original recovered routing
must already be withdrawn before allowing their reference release. Then replay
ReleaseAuthorised using the exact runtime receipt and persist acknowledgement.
The ordinary adoption path currently restores supervision but does not rebuild
local service backends. Recovered publication must obtain current container IP,
confirm policy and honour health checks before exposing a backend. Preserve
logical workload identity, original VIP/port and uncertain cleanup evidence.

Any new opener must be restricted to startup before consumers or concurrent
runtime registration. A task-drop test is not proof that a previous Bun process
and all its ingress consumers are gone. Keep actual Bun SIGKILL/host reboot and
remote catalogue acknowledgements as separate qualification gates.

## Completed: opt-in standalone startup recovery

New agent tests cover original VIP reservation before adopted publication,
unrecorded-runtime cleanup, original release-permission replay without launch,
and changed-runtime refusal before mutation. MockGrill has an explicit complete
runtime inventory override. Three positive contracts fail first (0.295s); changed-owner refusal already
passed the fenced stub. `recover_discovery_ownership` is now implemented in
`src/bun/agent/discovery_recovery.rs`; the first four focused native cases pass.
All five focused native cases pass (0.478s), including historical-health refusal.
Native affected tests/strict checks are running in `/tmp/rb-agent-recovery-native.log`;
the physical Linux case and affected tests/strict checks are running in
`/tmp/rb-agent-recovery-linux.log`. Do not edit Rust until both pipelines finish.

Implementation: a Recovered journal variant preserves startup proof
through consuming writes; open/correlate/persist before reserving exact original
service entries with no published backends. Reject clustered use and unknown
kernel keys. Confirm withdrawal with original VIP/port before enabling recovery.
Revalidate complete runtime evidence before adoption; restore policy, replay
ReleaseAuthorised, then run existing runtime reconciliation. Rebuild service
backends only for positively adopted runtimes, from current addresses and health
requirements. Retire empty original service allocations after confirmed cleanup.
Update MockGrill's inventory receipt when its release acknowledges, if needed.
The new physical `standalone_discovery_recovery_preserves_original_routing_and_cleanup`
case is undergoing qualification. It aborts the controller task, inserts an
unknown kernel key and requires refusal with the original route/hold intact;
then removes the fixture key, adopts the same generation, checks VIP traffic
and uses the existing retirement/reuse checks. It does not claim actual Bun death.
The controller JoinHandle is now optional, so a failed recovery cannot poll a
reaped task twice during fixture cleanup. Both active pipelines must finish
before further Rust edits. Native affected qualification passes: 315 tests (18.457s), strict Clippy and
formatting. The first Linux physical run fails (1.64s): InitPolicyGrill did not
delegate `adopt` to Runc, inheriting the unsupported default false response.
The wrapper now delegates actual Runc adoption. That exposed its second omitted
method: log_stem, so the saved record lacked the original generation's log
identity and Runc correctly refused (1.63s). The wrapper now delegates log_stem
too. The next full Linux pipeline uses `/tmp/rb-agent-recovery-linux-final.log`. The Linux pipeline is being
rerun in `/tmp/rb-agent-recovery-linux-fixed.log`; the initial log is retained.
Do not edit Rust until that pipeline finishes. The final focused physical case passes (26.02s), all 327 affected Linux tests
pass (23.916s), and strict Clippy/formatting pass on both platforms. Both compiled
pipelines finished. A full physical kernel rerun is active using the qualified
binary, because the wrapper delegation also affects earlier fixtures. Log:
`/tmp/rb-agent-recovery-physical.log`. No production caller selects this recovery profile.
Final full physical qualification passes: 74 cases (182.43s), including earlier
fixtures affected by the corrected wrapper. All pipelines finished. The feature
is committed separately, with README, book and progress updates. Actual Bun
death and remote acknowledgement remain separate gates. The chronological notes
above retain the earlier attempts; they do not indicate still-running processes.

## Completed: stable catalogue allocations

The replicated catalogue currently rebuilds VIPs from scratch on each report
change. Adding or removing a colliding service can move an existing service's
VIP. `EndpointCatalog::reconcile` and four failing-first tests are now added;
the checked reconciler is now implemented.
Tests cover a colliding arrival, a colliding departure, conflicting saved VIPs
and exhaustion (the old allocator silently shares a VIP when full).
All four fail first (0.557s); all twelve focused catalogue/builder tests then
pass (0.124s). The full native/Linux affected suites, seven agent-cluster cases
and three real placement/stop/remote-ingress cases plus strict checks are active
in `/tmp/rb-catalog-allocation-native.log` and `...-linux.log`. Do not edit Rust
until all pipelines finish. Native affected tests pass (596; 18.513s), all seven
agent-cluster cases pass (0.226s), and native strict checks pass. The first
integration invocation skipped the three ignored placement cases; an explicit
`--run-ignored only` native run now passes all three cases (38.800s) in
`/tmp/rb-catalog-allocation-native-placement.log`. Run the same explicit three
cases on Linux after its current pipeline finishes; do not count skipped cases.

Checked allocation reserves active original VIPs before newcomers and rejects
invalid/conflicting prior allocations and exhaustion. Rebuild returns Result
too; its old silent-sharing fallback is removed and callers/tests are updated.
The leader reconciles against committed `desired.endpoint_catalog` and includes
declared port-bearing desired services even when reports have no backends,
so temporary report loss cannot erase their allocations. Deleted-service
retirement still needs the remote acknowledgement ledger; this fix alone does
not authorise reuse after deletion. No protocol/state bump is needed for this
allocation algorithm alone.

Final qualification: 596 native/608 Linux affected library tests pass
(18.513s/31.753s), all seven agent-cluster cases pass on both (0.226s/0.209s),
and the three explicitly enabled multi-node cases pass (38.800s/38.651s).
Strict Clippy and formatting pass on both. All pipelines finished. Commit this
feature separately; no new format or production ownership selection is involved.
Earlier running/skipped notes above describe the initial invocations only.

For the subsequent remote authority, note that CouncilNode::write returns
Ok(CouncilResponse::Refused) for a committed refusal. The current publisher's
Ok(_) arm updates last_published anyway; never use that cache as acknowledgement
evidence. The retirement protocol must inspect actual success/committed state.

## Exact resumption point

Inspect the branch, PR checks and working tree. Finish any latest in-progress
continuation above before resuming item 1. The next core gate is durable discovery
publication and recovery integration; local ingress gates are recorded above.
The end of the [OCI plan](2026-09-20-oci-launch-ownership.md) records the next
transaction boundaries. Relevant code is in `src/bun/agent.rs`,
`src/bun/agent/egress_ownership.rs`, `src/bun/egress_owners.rs`,
`src/bun/discovery_owners.rs`, `src/onion/service_map.rs`,
`src/grill/runc_intent.rs` and
`src/grill/runc/owned.rs`. Physical regressions are in `tests/ebpf.rs` and
`tests/owned_runc.rs`.

`BunAgent::network_references` is intentionally only a captured live reference
map today. A recovered runtime hold with no original discovery reconciliation
refuses release. Do not fix that refusal by guessing a service VIP or assuming
that absent userspace metadata means no kernel/remote backend exists.

Use `CARGO_INCREMENTAL=0`. Do not edit Rust while any active pipeline can still
compile it. Reap/wait for those pipelines first. Serialise tests that manipulate
host networking, cgroups or eBPF. Keep the normal user for rootless runtime tests.

Native focused qualification:

```sh
CARGO_INCREMENTAL=0 cargo nextest run --lib \
  -E 'test(bun::agent::tests::) | test(sesame::firewall::tests::) | test(sesame::egress::tests::)'
CARGO_INCREMENTAL=0 cargo nextest run \
  --test compatibility --test runc_intent --test runc_commands --test owned_commands
CARGO_INCREMENTAL=0 cargo clippy --all-targets --all-features -- -D warnings
cargo fmt --check
```

If `cargo-nextest` is not on PATH, this session used
`/private/tmp/reliaburger-nextest-0.9.145/cargo-nextest` on macOS and
`/tmp/rb-nextest-0.9.145.*/cargo-nextest` inside the VM. These paths are temporary.

Linux environment:

```sh
limactl shell reliaburger-test bash
source "$HOME/.cargo/env"
cd /Users/miko/github/reliaburger
export CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0
export CARGO_TARGET_DIR=/home/miko.guest/reliaburger-release-target
```

Build physical test binaries as the ordinary VM user with `--features ebpf`;
obtain their exact executable paths from Cargo's JSON compiler-artifact output.
Run the kernel binary as root with `RELIABURGER_EBPF_TESTS=1`, `--ignored` and
`--test-threads=1`. Run the rootless binary as the ordinary user with
`RELIABURGER_ROOTLESS_RUNC_TESTS=1`. The laptop's `/tmp` and VM's `/tmp` are
separate: pass a host script through `limactl ... bash -s < script`.

Update the ledger, relevant book chapter, both READMEs when behaviour changes,
and this handoff after the next completed checkpoint. Record exactly which head
was tested and which release gates remain open.


## Latest continuation: committed catalogue publication

The scheduler now compares candidates with the current committed catalogue and
accepts only CouncilResponse::Applied as publication success. It no longer uses
a local cache that can suppress retries after refusal or another writer's update.
The real single-node-council regression fails first (8.040s), then passes while
also proving unchanged state causes no repeated log entries. All 257 affected
library tests and eight agent-cluster tests pass on native/Linux
(10.154s/10.357s and 5.034s/5.030s). Strict Clippy and formatting pass after a
Box::default test-style correction. Logs: /tmp/rb-catalog-commit-{red,native,linux}.log
and /tmp/rb-catalog-commit-strict-{native,linux}.log. No format change.

Next: persist the catalogue consumer census before serving endpoints, retain it
through snapshots/offline periods, and discharge a consumer only on explicit
operator decommission. This is a prerequisite for generation-specific withdrawal
acknowledgements, not permission to release any address yet. C34 and V01–V04
remain open. Hosted checks need inspection for every new commit.


## Latest continuation: durable catalogue consumer census

A node is committed to DesiredState.endpoint_consumers before its first catalogue
response. The bounded set survives snapshots and has no gossip/report expiry.
Decommission removes only the fenced identity and records that fact in its
immutable result; repeated decommission is idempotent and re-registration of the
retired name refuses. TLS certificate identity must match the path. Configured
credentials require the service principal; existing credential-free development
clusters may add consumer obligations but receive no release authority from this.

Three contracts failed first (0.560s), then passed (0.514s). Initial three-node
acceptance caught refusal of credential-free development requests: the original
native/Linux runs failed after 48.680s/48.596s. Preserving that existing profile,
with an explicit fourth API contract, fixes the regression. A failover fixture's
leader-authority probe now supplies its existing internal credential.

Final macOS/Linux qualification: 426 affected library tests (17.345s/28.295s),
ten agent-cluster/compatibility cases (5.488s/5.023s), three explicitly enabled
real placement cases (36.752s/38.699s), and two paused-worker/decommission
leader-failover cases (47.464s/47.080s). Strict all-target/all-feature Clippy and
formatting pass on both platforms. Logs: /tmp/rb-catalog-consumers-red.log,
/tmp/rb-catalog-consumers-focused.log, /tmp/rb-catalog-consumers-{native,linux}.log
(initial failure), /tmp/rb-catalog-consumers-final-{native,linux}.log and
/tmp/rb-catalog-consumers-failover-{native,linux}.log. All pipelines are reaped.
Protocol 15/state 28 require fresh pre-release clusters. C34/V01–V04 remain open.
