# Remaining work for 0.1.0 — 22 September 2026

The release is not ready. C34 remains the correctness blocker; V01–V04 are
acceptance gates. The installer, website, signing workflow and control-plane
withdrawal machinery exist. They do not replace end-to-end release qualification.

Completed in the consumer recovery session:

- `77e135f`: durable publication/receipt phases, permission-checked compaction,
  retained generation fences and bounded inventory (protocol 20/state 38,
  discovery schema 3).
- `82066b7`: enrolled consumer recovery, full-view kernel/userspace withdrawal,
  captured HTTP/WebSocket guard drainage, durable receipt permission and local
  lifecycle fencing. Recovery starts with empty views and refuses changed identity.
- `acc9b60`: authenticated current-leader receipt delivery, bounded rotating retry,
  exact 204 acknowledgement and explicit durable local confirmation. Blocked
  publication does not starve already-ready receipts.
- `d9db398`: actual kernel deletion/refusal and recovery tests; real mTLS reconciler
  qualification with a lost HTTP reply, leader change and lost local confirmation.

Qualification passes 406 affected library tests on each platform, three real
API TLS tests on each platform and all 80 physical Linux kernel tests (182.57s).
Strict all-target/all-feature Clippy passes on both platforms.

The consumer implementation is complete at the agent/reconciler boundary. Normal
clustered Bun startup still needs activation, service VIP retirement integration
and production qualification. Full-view replacement can briefly interrupt routing.

Standalone rootful kernel/discovery production activation, actual Bun OCI
interruption/reboot qualification, producer release/stale-report fences and
rolling-initialiser fixes remain complete. Detailed test results and outstanding
release checks live in the handoff.

Estimates are focused engineering effort, including regressions and ordinary
failure investigation. One day means about eight hours. Review, CI queues,
availability of clean hosts and soak duration add elapsed time. New failures can
extend the ranges.

| Remaining work | Difficulty | Estimated effort | Completion evidence |
| --- | --- | --- | --- |
| C34: clustered production activation and upgrade recovery | High | 1–2 days | Wire enrolled consumer recovery into normal clustered startup; complete service VIP retirement; qualify rootless integration and actual upgrade/rollback. Standalone rootful activation and reboot recovery are complete. |
| V01: complete live three-node acceptance | Medium–high | 0.5–1 day | Full supported catalogue, chaos, diagnostics, storage and cleanup matrix on independent Runc nodes. |
| V02: sustained TLS/storage/restart/upgrade qualification | High | 0.5–1 day, plus 24–48 hours running | Retain final-source/candidate evidence through sustained failures and recovery; investigate failures before acceptance. |
| V03: exact signed release candidate | Medium | 2–4 hours, plus review/merge and CI | Reviewed code/workflow reaches main; build once, stage through HTTPS, install and verify those exact bytes; promotion does not rebuild. |
| V04: repeated cold laptop installation matrix | Medium–high | 0.5–1.5 days, plus host availability | Clean supported hosts, including Intel macOS, reach a working cluster in under five minutes; retain timing and cleanup evidence. |

Producer release/stale-report protection is implemented in the opt-in durable
path (`887c77a`, `ac76b5c`). Its permanent bounded fence refuses new retirements
at capacity. Consumer recovery and receipt retry are implemented; normal clustered activation and production qualification remain above.

Working estimate: **roughly 3–6 engineering days**, plus external waits and soak time.
Re-estimate after normal clustered activation passes its full retirement cycle.
V05 dependency review is complete for the current lockfile with dated exceptions.
Optional refactors and explicitly deferred features are not new release blockers.
Fresh pre-release clusters, foreground-only native processes and the managed
Linux VM for macOS containers remain the agreed release scope.

For exact evidence and the next implementation entry point, use the
[session handoff](2026-09-20-v0.1.0-session-handoff.md) and
[remote withdrawal plan](2026-09-22-remote-withdrawal-ledger.md).
