# Remaining work for 0.1.0 — 22 September 2026

The release is not ready. C34 remains the correctness blocker; V01–V04 are
acceptance gates. The installer, website, signing workflow and control-plane
withdrawal machinery exist. They do not replace end-to-end release qualification.

Completed in this session:

- `dcbdd2a`: Linux command ownership records carry positive kernel boot identity.
- `c0c5c76`: OCI intent also carries boot identity; prior-boot launches are fenced,
  conflicting live kernel resources refuse cleanup, and stale OCI metadata is
  removed without signalling saved PIDs. Owner schema 3, OCI intent 5, protocol
  20/state 36.
- `9ec73ef`: actual abrupt VM reboot passes with a live container, prepared launch
  and retained address. Changed boot identity, runtime absence, unknown outcome,
  explicit release, successor execution and stale-release refusal are verified.
- `f151e94`, `cfec14b`: seven actual Bun death/cancelled-caller cases cover admission
  before intent, preparation, start, running init, retirement, adoption and rolling
  retry. The final focused matrix passes in 34.41s.
- `0d9b75d`: that matrix found and fixed rolling replacements skipping initialisers.
  Fresh and rolling deployments now share the owned init sequence; thirteen
  native rolling/init contracts pass, including failure containment.

These runtime results use the opt-in owned Runc path. The complete pinned-kernel
and durable-discovery startup path is still unfinished. Broader qualification passes 476 native/521 Linux affected library tests,
91 native/94 Linux integrations, all 74 physical kernel tests and the rootless
runtime suite. Strict Clippy and formatting pass on both platforms. Exact results, initial
failures/reruns and final-head hosted CI status are tracked in the session handoff.

Estimates are focused engineering effort, including regressions and ordinary
failure investigation. One day means about eight hours. Review, CI queues,
availability of clean hosts and soak duration add elapsed time. New failures can
extend the ranges.

| Remaining work | Difficulty | Estimated effort | Completion evidence |
| --- | --- | --- | --- |
| C34: consumer publication, recovery, confirmed withdrawal and receipt retry | High | 2–4 days | Persist original service/ingress exposures before publication; recover before publishing; confirm captured-request drainage; retry receipts and compact safely. |
| C34: durable kernel/discovery reboot recovery and production activation | High | 1–2 days | Reconcile missing prior-boot pins with original ownership and remote obligations; qualify the complete startup path, remaining runtime variants, and upgrade/rollback before normal selection. Rootful runtime crash/reboot evidence is already recorded. |
| V01: complete live three-node acceptance | Medium–high | 0.5–1 day | Full supported catalogue, chaos, diagnostics, storage and cleanup matrix on independent Runc nodes. |
| V02: sustained TLS/storage/restart/upgrade qualification | High | 0.5–1 day, plus 24–48 hours running | Retain final-source/candidate evidence through sustained failures and recovery; investigate failures before acceptance. |
| V03: exact signed release candidate | Medium | 2–4 hours, plus review/merge and CI | Reviewed code/workflow reaches main; build once, stage through HTTPS, install and verify those exact bytes; promotion does not rebuild. |
| V04: repeated cold laptop installation matrix | Medium–high | 0.5–1.5 days, plus host availability | Clean supported hosts, including Intel macOS, reach a working cluster in under five minutes; retain timing and cleanup evidence. |

Producer release/stale-report protection is implemented in the opt-in durable
path (`887c77a`, `ac76b5c`). Its permanent bounded fence refuses new retirements
at capacity. Full consumer integration and production qualification remain above.

Working estimate: **5–10 engineering days**, plus external waits and soak time.
Re-estimate after the complete remote cleanup loop passes crash/recovery tests.
V05 dependency review is complete for the current lockfile with dated exceptions.
Optional refactors and explicitly deferred features are not new release blockers.
Fresh pre-release clusters, foreground-only native processes and the managed
Linux VM for macOS containers remain the agreed release scope.

For exact evidence and the next implementation entry point, use the
[session handoff](2026-09-20-v0.1.0-session-handoff.md) and
[remote withdrawal plan](2026-09-22-remote-withdrawal-ledger.md).
