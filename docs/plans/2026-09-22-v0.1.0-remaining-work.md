# Remaining work for 0.1.0 — 22 September 2026

C34 implementation and final qualification are complete. [CI](https://github.com/reliaburger/reliaburger/actions/runs/35750394293) and
[Build & Release](https://github.com/reliaburger/reliaburger/actions/runs/35750394720) pass at `6c64fed`. The final closure commit changes
only documentation. The release is not ready: V01–V04 still need independent
acceptance evidence. Rootless Runc is standalone only; container clusters use
rootful Linux Runc/eBPF.

Completed in the activation session:

- Normal enrolled rootful Runc/eBPF startup recovers consumer ownership before
  adoption. Standalone rootless startup recovers original execution and forwarding.
- Local services use council VIPs. Retirement waits for consumer guards and exact
  runtime release proof; repeated local removal is idempotent.
- Startup retains stopped executions and reserved ports until remote cleanup can
  progress. Discovery polling continues during pending deployments.
- Actual rootful and rootless OCI upgrade, explicit rollback and failed-candidate
  automatic revert preserve the live execution; confirmed cleanup succeeds.
- Three enrolled OCI nodes pass six controlled leader-last node API swaps and final
  remote retirement (76.54s). This fixture uses one isolated Linux host; the full
  independent-host catalogue and rolling-controller acceptance remain V01/V02.
- Protocol 20/state 39/discovery schema 4 are current. Earlier development state
  requires a fresh cluster. Standalone complete host reboot remains qualified.

The final audit adds actual Bun death during automatic application restart before
replacement adoption is published (19.54s), then repeats the complete OCI driver
and strict Linux/macOS checks. The parent checkboxes now match their committed
production integration. The closure record and session handoff preserve exact
local and hosted evidence. No compiler/test pipeline remains active locally.

Estimates are focused engineering effort, including regressions and ordinary
failure investigation. One day means about eight hours. Review, CI queues,
availability of clean hosts and soak duration add elapsed time. New failures can
extend the ranges.

| Remaining work | Difficulty | Estimated effort | Completion evidence |
| --- | --- | --- | --- |
| V01: complete live three-node acceptance | Medium–high | 0.5–1 day | Full supported catalogue, chaos, diagnostics, storage and cleanup matrix on independent Runc nodes. |
| V02: sustained TLS/storage/restart/upgrade qualification | High | 0.5–1 day, plus 24–48 hours running | Retain final-source/candidate evidence through sustained failures and recovery; investigate failures before acceptance. |
| V03: exact signed release candidate | Medium | 2–4 hours, plus review/merge and CI | Reviewed code/workflow reaches main; build once, stage through HTTPS, install and verify those exact bytes; promotion does not rebuild. |
| V04: repeated cold laptop installation matrix | Medium–high | 0.5–1.5 days, plus host availability | Clean supported hosts, including Intel macOS, reach a working cluster in under five minutes; retain timing and cleanup evidence. |

Producer release/stale-report protection, durable consumer recovery and receipt
retry are implemented. Rootless cluster support is explicitly deferred: surviving
execution intent alone cannot authorise reuse of an old remotely published host
port after restart. Container clusters use rootful Linux Runc/eBPF; rootless Runc
is standalone only, and Bun enforces that boundary before ownership recovery.

Working estimate: **roughly 2–4 engineering days**, plus external waits and the
24–48-hour soak. These estimates remain conditional on candidate qualification and
acceptance findings. Implementing rootless clusters is outside the agreed 0.1.0 scope.
V05 dependency review is complete for the current lockfile with dated exceptions.
Optional refactors and explicitly deferred features are not new release blockers.
Fresh pre-release clusters, foreground-only native processes and the managed
Linux VM for macOS containers remain the agreed release scope.

For exact evidence and the next implementation entry point, use the
[session handoff](2026-09-20-v0.1.0-session-handoff.md) and
[remote withdrawal plan](2026-09-22-remote-withdrawal-ledger.md).
