# C34 final evidence and checklist closure

C34 is complete on PR167. Protocol 20/state 39 remain current.
The verified source checkpoint is `6c64fed7ee9a0560dccffa7360252e3274e18055`:
[CI](https://github.com/reliaburger/reliaburger/actions/runs/35750394293) and [Build & Release](https://github.com/reliaburger/reliaburger/actions/runs/35750394720) both pass.
All following closure changes are documentation only.

The formerly open parent checkboxes preserved earlier integration boundaries.
They are now closed against their completed production paths and physical tests.
Historical paragraphs remain dated evidence, not current outstanding tasks.

| Contract | Implementation and committed qualification |
| --- | --- |
| Foreground process identity and retirement | Production `ProcessGrill` owner/gate and pre-adoption reconciliation; `tests/process_owner.rs`, `tests/process_recovery.rs`, `tests/job_recovery.rs`; actual caller/Bun death, generation refusal, descendant cleanup and upgrades. |
| External commands and original OCI intent | `OwnedCommands`, generation claims and whole-operation workers in `src/grill`; `tests/owned_commands.rs`, `tests/runc_intent.rs`, `tests/owned_runc.rs`; actual cancelled preparation and killed caller. |
| Runc launchers and rootless helpers | Owned rootful and standalone rootless production selection; `tests/owned_runc.rs`, `tests/owned_rootless.rs`; durable helper identity, original exit/log evidence and positive retirement. Rootless clusters explicitly refuse (`b41cdd9`). |
| Namespace, links, forwarding and address holds | Generation-owned network commands; `tests/owned_network.rs`, held-address and prior-boot cases in `tests/owned_runc.rs`; actual caller death and separately recorded host reboot. |
| Kernel/discovery recovery and publication | Durable source/service/backend journals, persistent BPF maps and captured-request guards; `tests/ebpf.rs`, library refusal/recovery cases, normal production startup/recovery in `tests/oci_crash.rs`. |
| Cluster activation, VIP retirement and upgrades | Enrolled consumer identity, council VIPs, remote withdrawal/receipt retry; actual one/three-node OCI cases in `tests/oci_crash.rs`, including six node API swaps and final retirement. |

The audit found and closed one physical qualification gap: Bun SIGKILL
during an automatic application restart after execution starts but before its
replacement adoption record is published. Existing seven-boundary interruption
coverage exercised initial deployment and explicit retry; ordinary automatic
restart had been qualified with a live controller, but not this death boundary.
The new actual-Bun regression passes in 19.54s: a test-only fsync gate pauses
replacement adoption after execution starts. Recovery retires that unrecorded
generation, preserves the kernel owner and permits explicit redeployment. Final
service/reference inventories are empty. The privileged OCI driver includes it.

Final local qualification also passes the entire OCI interruption driver: 14
owned-Runc entries (13.20s), eight Bun entries (177.24s), four network entries
(1.32s), strict Linux/macOS Clippy and formatting. Manual host reboot is the
separate earlier qualification; the driver does not reboot the CI host.
Evidence is retained in `/tmp/rb-c34-final-linux.log` and the disposable VM's
`/var/tmp/reliaburger-oci-interruptions.cJulVs`.

Hosted qualification passes portable Linux/macOS, minimum Rust with default and
no-default features, rootless/privileged runtime and kernel tests, cluster and
upgrade suites, coverage, benchmarks, 10k-member scale acceptance, executable
examples, packaging, PDF and all four binary targets. Release validation and
candidate creation correctly skip on this PR event. No candidate was published.
Earlier superseded runs were cancelled, not counted as successful qualification.

V01 independent-host full catalogue, V02 sustained recovery, V03 exact signed
candidate and V04 cold installs remain separate. Passing C34 does not close them.
